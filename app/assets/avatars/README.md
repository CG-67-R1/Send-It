# Avatar images (legacy)

Avatars are now loaded from **`app/avatar/`**. See that folder for the current set (devil, donkey, flip, goat, no_face frames, the_goat). This folder is no longer used by the app.
