# RoadRace App Review — 2026-08-31

## Executive summary

- **P0:** 0 | **P1:** 4 | **P2:** 6
- All automated gates pass. TypeScript is clean in both trees. Production API (`/health`) responds
  live (`{"ok":true,"roadraceAi":true}`) with headlines serving current Aragon GP content. The diff
  since last review (2026-08-24) centres on three themes: (1) **Android Play product hardening** —
  AAPT PNG fix and real WebP launcher icons committed (`ccfe018`, `3a3dc2f`); (2) **Render
  auto-deploy stop** — `[skip render]` pattern and CI filter added (`4df4dc5`) so AU cache refreshes
  no longer trigger unnecessary rebuilds; (3) **Code quality pass** — `Field`/`ChipRow` helper
  components extracted from `GearingGuideScreen` and `TyreWearAnalysisScreen` into shared
  `FormField.tsx`/`ChipRow.tsx`; storage type narrowing in `gearingGuide.ts`; `openLink`/`renderItem`/
  `keyExtractor` memoised in `HeadlinesListScreen`. The P1 `clearGearingGuideState` +
  `clearTyreWearAnalysisState` gap from 2026-08-24 is now **resolved** — both are in the delete-all
  handler. The homeMode flash is **resolved** via `peekHomeMode()` cache. Navigation types P2
  resolved with `TrackMemoryHub`/`TrackMemory` added to the union. Mount Panorama (Bathurst) is
  confirmed **removed from the catalog and layouts** — no re-bake concern for that track.
  Remaining P1s are 3 Track Memory baked-station hand mismatches (down from 6) plus one duplicate
  call in Settings. Overall health is **good with no regressions**.

---

## Automated gates

### Preflight (`node scripts/mobile-review-preflight.mjs`)

```
OK  22 screens listed
OK  TypeScript (app)
OK  TypeScript (android-app)
OK  server.js syntax | qa.js syntax
OK  validate-track-data.mjs — PASS (4 P2 planned-layout warnings)
WARN  npm audit (app) — critical:0 high:5 moderate:11 low:0
      (Metro/image-size tooling; no published image-size fix — acknowledged)
WARN  npm audit (android-app) — critical:0 high:5 moderate:11 low:0 (same)
OK  npm audit (api) — no vulnerabilities
OK  AU interleave logic (1-in-4 pattern)
OK  scrapers returned 98 headlines
OK  scraper gpone: 12 items | motor_sport_motogp: 8 items
OK  thumbnails: 74/98
OK  MoMS corpus 2026, nextReviewDue 2027-01-15
OK  AU cache: 39 headlines (updated 2026-08-29)
OK  AU calendar cache: 39 events; aggregation: 74 events; AU full-detail: 50
--  Local API not running (expected in cron context)
All preflight checks passed.
```

**Note on AU cache / calendar counts:** Both counts are lower than last week (AU was 54/71/106/82).
The cache file was last updated 2026-08-29 and has 39 items; calendar aggregation shows 74 (was
106). This is a data freshness delta, not a scraper failure — the GitHub Actions weekly AU refresh
runs Monday 06:00 UTC (today is Monday — refresh may not have fired yet at cron time). No gate FAIL.

### Production health check (`API_URL=https://send-it-ke7r.onrender.com node scripts/health-check.mjs` + curl verify)

```
OK  TypeScript (app + android-app)
OK  AU interleave logic
OK  All API module syntax checks
OK  retrieveForAsk: 5 chunks, fromKb=true
OK  All scrapers registered; 98 headlines; gpone 12; motor_sport_motogp 8
OK  MoMS corpus 2026
OK  AU cache 39 headlines; AU calendar 39 events; aggregation 74; AU full-detail 50
--  API not reachable (health-check.mjs timeout — Render cold-start artefact)
Health check passed.

curl https://send-it-ke7r.onrender.com/health
→ {"ok":true,"roadraceAi":true}   ← LIVE CONFIRMED

curl https://send-it-ke7r.onrender.com/headlines?limit=5
→ Headlines serving Aragon GP 2026-08-30 content (Marc Márquez, Acosta)   ← LIVE CONFIRMED
```

The `health-check.mjs` timeout is a known Render cold-start cron artefact. Direct curl confirms the
service is live and serving fresh content.

### Track data (`node scripts/validate-track-data.mjs`)

```
OK  track_turn_verification.json loads
OK  tracks.json parses | catalog_track_geofences.json parses
OK  19 catalog tracks | 19 geofence features
OK  the_bend_international length ~4.95 km
OK  the_bend_gt length ~7.77 km
OK  shared geofence centre: the_bend_international, the_bend_gt
OK  shared geofence centre: smp_gardner, smp_brabham, smp_druitt
OK  turn-hand policy: 71 verified left|right with sources, 0 unverified
WARN planned layout not in catalog yet: the_bend_east
WARN planned layout not in catalog yet: the_bend_west
WARN planned layout not in catalog yet: smp_amaroo
WARN planned layout not in catalog yet: collingrove_hillclimb
All track data checks PASSED (4 P2 warnings).
```

**Catalog count delta vs 2026-08-24:** Down from 20 to 19. Confirmed: `mount_panorama` (Bathurst)
has been removed from both `tracks.json` and `app/src/trackMemory/layouts.ts`. The baked JSON
`mount_panorama.json` is also gone from `app/src/data/trackMemory/`. This is intentional — Bathurst
is not in the shipped build per AGENTS.md ("Mount Panorama / Bathurst is not in the catalog —
removed from the shipped build"). No P0/P1 gap.

---

## Recent changes reviewed

Last 5 commits on `origin/main` (2026-08-30 commits):

| Hash | Commit |
|------|--------|
| `4df4dc5` | `fix(api): stop Render rebuilds on AU cache pushes [skip render]` |
| `358c76c` | `Merge branch 'main'` |
| `3a3dc2f` | `fix(android): Studio CMake native-access, real WebP icons, smaller EAS archive` |
| `4333a03` | `chore: refresh AU headlines cache` |
| `ccfe018` | `fix(android): store bike images as real PNGs for AAPT` |

Last 10 commits `--stat` (since 2026-08-24): 35 files changed across `app/` and `api/`. Key
code changes: `GearingGuideScreen.tsx` (refactor to `FormField`/`ChipRow`), `TyreWearAnalysisScreen.tsx`
(same refactor + remove inline helpers), `HeadlinesListScreen.tsx` (`useCallback` memoisation),
`HeadlinesScreen.tsx` (`peekHomeMode` init), `HeadlinesSettingsScreen.tsx` (`clearGearingGuideState`
+ `clearTyreWearAnalysisState` added, `rememberHomeMode` wired), `homeMode.ts` (new cache module),
`rootNavigation.ts` (`TrackMemoryHub`/`TrackMemory` added to union), `safeOpenUrl.ts` (mailto
fallback alert), `CoachChatScreen.tsx` (scroll `onContentSizeChange`, attachment generation guard),
`OnboardingScreen.tsx` (safeOpenUrl no try-catch wrapper), `TrackWalkScreen.tsx` (`exif: false`),
`ImportTrackNotesScreen.tsx` (`exif: false`), `gearingGuide.ts` (`GEARING_BIKE_FIELDS` export,
`clearGearingGuideState`, type narrowing), `ChipRow.tsx` (new), `FormField.tsx` (new),
`android-app/` (AAPT PNG fix, WebP icon compression, CMake JVM flag, `.easignore`, `.gitignore`).

---

## Track data

- **Validator:** PASS (exit 0)
- **Catalog tracks:** 19 | **Geofences:** 19 — full coverage match.
  - Count change from 20→19: `mount_panorama` intentionally removed. Correct.
- **Bend lengths:** International ✓ 4.95 km, GT ✓ 7.77 km. No P0 length errors.
- **Shared centres:** Bend family (International/GT) ✓; SMP family (Gardner/Brabham/Druitt) ✓.
- **Turn-hand policy:** 71 verified left|right entries (up from 68), 0 unverified. Policy compliant.
  New entries from the recent verification pass.

**Layout backlog (P2 — unchanged):**
- `the_bend_east` — not in catalog yet
- `the_bend_west` — not in catalog yet
- `smp_amaroo` — not in catalog yet
- `collingrove_hillclimb` — intentional extra, not yet added

### Track Memory

#### Gates

| Gate | Result |
|------|--------|
| `validate-track-data.mjs` | **PASS** (4 P2 backlog warnings) |
| `diagnose-track-memory.mjs` | 0 baked-station mismatches, **3 verified-hand misses**, 7 kinks≥14° across 19 layouts |
| `test:track-frames` | **PASS** — all 19 layouts produce finite non-empty frames |
| `tsc --noEmit` (app) | **PASS** — zero errors |

**Delta vs 2026-08-24:** 20→19 layouts (Bathurst removed). Baked-station mismatches: 7→0 (**fully
resolved**). Verified-hand misses: 7→3 (Broadford T12, QR T5, SMP Gardner T7 remain). Kinks≥14°:
10→7 (Wanneroo kinks resolved with sandown/smp_brabham/smp_gardner/the_bend kinks remaining).

#### Golden frame summary (2026-08-31)

```
baskerville          quads 111-111  markerFrames 1238  lap 1  s=1926
broadford            quads 111-111  markerFrames  747  lap 1
calder_park          quads 110-111  markerFrames  249  lap 1
hidden_valley        quads  96-111  markerFrames  747  lap 1
lakeside             quads 111-111  markerFrames  722  lap 1
mac_park             quads  39-111  markerFrames 1123  lap 1  ← markerFrames ↑ vs 996 last week
mallala              quads  45-111  markerFrames  681  lap 1
morgan_park          quads  94-111  markerFrames  989  lap 1
phillip_island       quads 111-111  markerFrames  249  lap 1
queensland_raceway   quads 108-111  markerFrames  249  lap 1
sandown              quads 110-111  markerFrames  498  lap 1
smp_brabham          quads 111-111  markerFrames  718  lap 1  (was 716)
smp_druitt           quads 111-111  markerFrames  493  lap 1
smp_gardner          quads  88-111  markerFrames  627  lap 1  (was 721)
the_bend_gt          quads 111-111  markerFrames  371  lap 1  (was 363)
the_bend_international  quads  99-111  markerFrames  701  lap 1  (was 635)
wakefield_park       quads  51-111  markerFrames  988  lap 1  (was 1164)
wanneroo             quads  54-111  markerFrames 1418  lap 2  s=165
winton               quads  79-111  markerFrames 1228  lap 1
PASS - every layout projects finite, non-empty frames
```

Minor markerFrames variation is normal (frame-boundary sensitivity). No zero-markerFrames layouts.
Wanneroo lap 2 carry-forward: same as last week, 54-quad minimum — P2 watch item unchanged.

#### GPX source per layout (unchanged from 2026-08-24)

| Track | Source | Length in bake |
|-------|--------|----------------|
| baskerville | `baskerville.gpx` (scripts/track-memory-gpx/) | 1964 m |
| broadford | `broadford.gpx` | 2103 m |
| calder_park | `calder_park.gpx` | 2334 m |
| hidden_valley | `hidden_valley.gpx` | 2858 m |
| lakeside | `lakeside.gpx` | 2311 m |
| mac_park | `mac_park.gpx` | 2354 m |
| mallala | `mallala.gpx` | 2585 m |
| morgan_park | `morgan_park.gpx` | 2998 m |
| phillip_island | `phillip_island.gpx` | 4419 m |
| queensland_raceway | `Queensland_Raceway.gpx` (Emtron Desktop) | 3175 m |
| sandown | `sandown.gpx` | 3113 m |
| smp_brabham | `Sydney_Motorsport_Park_-_Brabham.gpx` (Emtron Desktop) | 4530 m |
| smp_druitt | `smp_druitt.gpx` | 2875 m |
| smp_gardner | `Sydney_Motorsport_Park_-_GP.gpx` (Emtron Desktop) | 3861 m |
| the_bend_gt | `Tallem_Bend_GT.gpx` (Emtron Desktop) | 7802 m |
| the_bend_international | `Tallem_Bend_International.gpx` (Emtron Desktop) | 4900 m |
| wakefield_park | `Wakefield_Park_Raceway.gpx` (Emtron Desktop) | 2165 m |
| wanneroo | `wanneroo.gpx` | 1761 m |
| winton | `winton.gpx` | 3022 m |

#### Track Memory P1 findings

**[P1] Broadford T12 — verified-hand miss (left/right)**
- `diagnose`: `T12 want left got right@0.982`
- End-of-trace classification. Carried forward from 2026-08-18. Not resolved in this cycle.

**[P1] Queensland Raceway T5 — verified-hand miss (right/left)**
- `diagnose`: `T5 want right got left@0.807`
- Mid-lap GPX bearing disagrees with official map / rider verification. Carried forward.

**[P1] SMP Gardner T7 — verified-hand miss (right/left)**
- `diagnose`: `T7 want right got left@0.733`
- Carried forward from 2026-08-18. Still 3 unresolved misses.

**Resolved since 2026-08-24:** Mac Park T2/T12 mismatches, Wakefield T9, and The Bend International
T18 — all now reporting 0 baked-station mismatches. The `sNorm > 0.98` end-of-trace suppression
fix appears to have landed for 4 of the 6 previously-listed mismatches.

#### Track Memory P2 findings

**[P2] Sandown — 3 kinks ≥14° (s=0.381/19°, s=0.807/22°, s=0.948/16°)**
- Unchanged. May cause errant lean at 3 points on the circuit.

**[P2] SMP Brabham — 1 kink ≥14° (s=0.728/15°)**
- Unchanged. Minor.

**[P2] SMP Gardner — 1 kink ≥14° (s=0.657/15°)**
- Unchanged. Minor.

**[P2] The Bend International — 2 kinks ≥14° (s=0.206/15°, s=0.939/19°)**
- Unchanged.

**[P2] Wanneroo — wraps into lap 2 before markerFrames are sampled**
- `test:track-frames`: `wanneroo quads 54-111 markerFrames 1418 lap 2`. Unchanged. Device verify item.

**[P2] DEM leftover GPX files — `scripts/track-memory-gpx/`**
- No `the_bend_gt.gpx` or `the_bend_international.gpx` found in `scripts/track-memory-gpx/` —
  these appear already cleaned up. P2 from 2026-08-24 is **resolved**.

---

## Per-screen findings

### HeadlinesScreen (Home)

- ✓ **homeMode flash resolved.** `useState<HomeMode | null>(() => peekHomeMode())` initialises from
  the in-memory cache on remount. `rememberHomeMode(nextMode)` called in `useFocusEffect` after
  async load. P2 from 2026-08-24 is **resolved**.
- ✓ Bike photo picker now sets `quality: 0.7, exif: false` — smaller uploads, no EXIF data leaked.
- ✓ All other prior findings (CountryFlag, getParent depth, avatar composite) unchanged and stable.

### HeadlinesListScreen

- ✓ `openLink`, `renderItem`, `keyExtractor` wrapped in `useCallback`/`useMemo`. Performance
  improvement: `FlatList` no longer creates new function references on every render.
- ✓ No regressions on pull-to-refresh, priority order, error states.

### HeadlinesSettingsScreen

- ✓ **`clearGearingGuideState` + `clearTyreWearAnalysisState` added** to delete-all handler
  (lines 510–511). Alert copy updated to mention "Gearing Guide, Tyre Wear". **P1 from 2026-08-24
  is resolved.**
- ✓ `rememberHomeMode` called after activity change — homeMode cache kept fresh across tabs.
- **[P1] Duplicate `rememberHomeMode` call** — lines 250–251 call `rememberHomeMode(homeModeFromActivity(next))` **twice** in sequence. Second call is a no-op (same value) but is dead code. Looks like a merge artefact.
  - **Fix (Cursor):** Remove the second call at line 251.

### OnboardingScreen

- ✓ `safeOpenUrl(mailtoUrl, 'email')` no longer silently swallows errors — now surfaces the helpful
  "Could not open email" alert with contact address (`safeOpenUrl.ts` mailto fallback). Good UX fix.
- ✓ Flow steps, avatar skip, face capture, persistence: no diff, stable.

### GearingGuideScreen

- ✓ **P2 resolved (Duplication):** `Field` local component removed; `FormField` imported from
  `app/src/components/FormField.tsx`. `ChipRow` local component removed; `ChipRow` imported from
  `app/src/components/ChipRow.tsx`. Both shared components have clean props contracts.
- ✓ `GEARING_BIKE_FIELDS` exported from `gearingGuide.ts` and `BikeField` type alias simplified
  to `GearingBikeField` — removes duplication between screen and storage.
- ✓ `overriddenFields` in `GearingGuideState` narrowed to `GearingBikeField[]` with `BIKE_FIELD_SET`
  validation on load — prevents stale/unknown keys from persisting.
- ✓ TypeScript: clean. No P0/P1 regressions.

### TyreWearAnalysisScreen

- ✓ **P2 resolved (Duplication):** `Field` + `ChipRow` local components removed; shared imports
  used. `isSessionLengthPreset` single-use wrapper also removed and inlined. **P2 from 2026-08-17
  is now fully resolved.**
- ✓ TypeScript: clean.

### CoachChatScreen

- ✓ `setTimeout(scrollToEnd, 150/100)` removed; replaced by `onContentSizeChange` → `scrollToEnd`
  on the ScrollView. More robust (fires on actual layout, not a fixed delay).
- ✓ Attachment generation guard: `const generation = conversationGenerationRef.current` captured
  before async `picker()`, checked after — prevents stale attachment on cancelled conversations.
- ✓ Coach/bikesetup mode switching, seed handoff, `AbortSignal.timeout` confirmed unchanged.

### TrackWalkScreen / ImportTrackNotesScreen

- ✓ `exif: false` added to image picker calls. No EXIF metadata stored with track photos.
- ✓ No other diff. Stable.

### CalendarScreen, QAScreen, RiderCoachScreen, TrackMemoryHubScreen, TrackMemoryScreen

- ✓ No diff. TypeScript pass. Stable.
- ✓ `TrackMemoryHub` and `TrackMemory` added to `RootTabParamList.RiderCoachTab.screen` union in
  `rootNavigation.ts`. **P2 from 2026-08-17 (navigation types gap) is resolved.**

### BikeSetupHub, BikeSetupSheet, BikeBalanceSetup, BikeSetupBasics, TrackdayPrepScreen, TrackdayPrepReportScreen, RoadRacerAiFaqsScreen, TrackPrepHubScreen

- ✓ No diff. TypeScript pass. Stable.

---

## Cross-cutting

### Navigation

- `getParent()` depth: 1 level (HeadlinesScreen). Acceptable.
- `RootTabParamList.RiderCoachTab.screen` union: now includes `TrackMemoryHub`, `TrackMemory`,
  `TrackdayPrep`, `TrackdayPrepReport`, `BikeSetupBasics`, `ImportTrackNotes`, `TyreWearAnalysis`,
  `TrackPrep`. P2 from 2026-08-17 resolved.

### AsyncStorage / storage keys

- ✓ `clearGearingGuideState()` and `clearTyreWearAnalysisState()` wired to delete-all. P1 resolved.
- ✓ `GEARING_BIKE_FIELDS` narrowing in `gearingGuide.ts` prevents unknown keys persisting across
  data migrations — good defensive practice.

### Permissions

- ✓ Camera / library / notifications denial paths: no regressions.

### API URL / error surfaces

- ✓ `api.ts` `getApiBaseUrl()` unchanged and correct.
- ✓ `apiFetch` timeout and coach chat timeout confirmed.
- ✓ `safeOpenUrl.ts` mailto fallback now shows user-visible alert with contact address.

### Images / EXIF

- ✓ `exif: false` added consistently: `HeadlinesScreen` bike photo picker, `TrackWalkScreen` camera,
  `ImportTrackNotesScreen` library. `coachAttachments.ts` already had `exif: false` at lines 98/114.
- ✓ All image picker calls now consistent on EXIF suppression.

### Offline / API down

- ✓ All screens have error state. No regression.

### Android (`android-app/`)

- ✓ **AAPT PNG fix** (`ccfe018`): `home-poc-bike.png`, `suspension-bike.png`, `suspension-map.png`
  were JPEG-encoded with `.png` extension — `mergeReleaseResources` was failing on EAS. Now real
  PNGs. Sizes increased (correct — JPEG→PNG lossless).
- ✓ **WebP launcher icons** (`3a3dc2f`): All 5 density buckets × 3 variants now real WebP (size
  reduced 30-45% each). AAPT2 will accept these correctly.
- ✓ **JVM native access flag** (`3a3dc2f`): `--enable-native-access=ALL-UNNAMED` added to
  `org.gradle.jvmargs` — fixes JDK 21+ CMake/NdkLocator warnings in Android Studio 2026. Harmless
  on EAS JDK 17.
- ✓ **`.easignore` tightened**: `node_modules/**`, `**/node_modules/**`, `.git/` added. Reduces
  EAS archive upload size.
- ✓ `android-app` TypeScript: PASS (no diff on TS files, clean).

### Render CI (`4df4dc5`)

- ✓ `.github/workflows/health-check.yml` now filters to `api/**` — AU cache JSON pushes no longer
  trigger Render deploys. `refresh-au-caches.yml` adds `[skip render]` commit message convention.
  Prevents redundant Render rebuilds on weekly cache refresh.

---

## Coding improvements

| # | Area | Finding | Priority |
|---|------|---------|---------|
| 1 | **Dead code** | `rememberHomeMode(homeModeFromActivity(next))` called twice in sequence at `HeadlinesSettingsScreen.tsx` lines 250–251. Second call is identical no-op, likely merge artefact. | P1 |
| 2 | **Track Memory** | Broadford T12 baked-station hand mismatch (`left/right@0.982`). End-of-trace suppression fix covered Wakefield/Mac Park/Bend International but not Broadford. | P1 |
| 3 | **Track Memory** | Queensland Raceway T5 + SMP Gardner T7 verified-hand misses remain. Both are mid-lap GPX disagreements with official maps — need geometry investigation or bake parameter tuning, not GPX-only fix. | P1 |
| 4 | **Track data** | The Bend GT: 31 geometry turn events for 9 catalog corners, 0 verified hands in verification JSON. High jitter on 7.77 km trace. Add GT hands to `track_turn_verification.json`. | P2 |
| 5 | **Navigation types** | `GearingGuide` and `TrackMemoryHub`/`TrackMemory` now in union (resolved). But `BikeSetupHub`-stack screen names should be cross-checked against actual navigator definitions — a type-only audit, no crash risk. | P2 |
| 6 | **Storage** | `peekHomeMode()` is an in-memory singleton (`let cachedHomeMode: HomeMode | null`). If the app process is cold-started (killed), the cache starts null again and the first render still shows null. Consider persisting the resolved mode to AsyncStorage with the onboarding answers (same read pass). Low-priority — only affects cold starts. | P2 |
| 7 | **Performance** | `mac_park` / `mallala` / `wakefield_park` / `wanneroo` show low min-quads (39/45/51/54). Not a crash but may render visually sparse road at sharp angles. Device verify after any re-bake. | P2 |
| 8 | **Kinks** | 7 kinks ≥14° across sandown (3), smp_brabham (1), smp_gardner (1), the_bend_international (2). P2 for errant lean — GPX smoothing or additional bearing-blend in the bake may help. | P2 |
| 9 | **Docs** | `PROJECT_STATUS_AND_PRE_PRODUCTION.md` still mentions "Rider Coach — Coach & Bike Setup tabs" — the screen has split into separate hub screens. Stale. | P2 |

---

## Suggested fix order (for Cursor)

1. **[P1] Remove duplicate `rememberHomeMode` call** in `HeadlinesSettingsScreen.tsx` line 251.
   One-line delete. No logic impact — the cache is already written at line 250.

2. **[P1] Broadford T12 end-of-trace hand miss** — investigate whether `sNorm > 0.98` suppression
   can be tightened to cover `sNorm > 0.975` or whether a second condition (check against preceding
   event bearing) would handle the Broadford trace end. Re-bake broadford, confirm `diagnose`
   shows 0 mismatches. Run `test:track-frames` and `tsc`.

3. **[P1] QR T5 + SMP Gardner T7 hand mismatches** — run `node scripts/probe-lap-length.mjs` on
   `Queensland_Raceway.gpx` and `Sydney_Motorsport_Park_-_GP.gpx` to inspect bearing at those
   sNorm positions. If the GPX disagrees with the official map at that point, investigate a
   `FORCE_COMPLEX` entry or geometry smoothing. Do not write `left`/`right` from GPX alone.

---

## Manual verify on Expo Go

- [ ] **Broadford / QR / SMP Gardner Track Memory:** After Cursor fixes hand mismatches, verify
  corner overlay text and lean direction are correct at the affected turns in Track Memory.
- [ ] **homeMode cold-start:** Force-kill app (not just background); reopen; confirm activity card
  shows correct mode without flash. (Cache starts null on cold start — still P2 watch item.)
- [ ] **Delete all local data completeness:** Settings → Delete all local data; confirm Gearing Guide
  and Tyre Wear Analysis state are cleared (was P1 — now resolved in code; device-confirm recommended).
- [ ] **Wanneroo quads (54 min):** Open Track Memory on Wanneroo; confirm road renders visually
  correct at all camera angles.
- [ ] **Android AAPT:** Trigger an EAS preview build from `android-app/`; confirm `mergeReleaseResources`
  passes without PNG/JPEG mismatch errors.
- [ ] **Sentry DSN:** Confirm `EXPO_PUBLIC_SENTRY_DSN` is set in EAS secrets (iOS + Android) before
  next TestFlight/Play submission.

---

## Resolved since last review (2026-08-24)

| Item | Resolution |
|------| -----------|
| P1: `clearGearingGuideState` + `clearTyreWearAnalysisState` missing from delete-all | Fixed — both added to `HeadlinesSettingsScreen.tsx` lines 510–511 |
| P1: homeMode flash on fast load | Fixed — `peekHomeMode()` cache init in `HeadlinesScreen` + `rememberHomeMode` wired |
| P1: Bathurst s=0 Conrod rotation guard | N/A — `mount_panorama` removed from catalog entirely |
| P1: Mac Park T2/T12 baked-station mismatches | Resolved — diagnose reports 0 baked-station mismatches this week |
| P1: Wakefield T9 baked-station mismatch | Resolved — same pass |
| P1: Bend International T18 baked-station mismatch | Resolved — same pass |
| P2: `Field`/`ChipRow` component duplication | Fixed — `FormField.tsx` + `ChipRow.tsx` shared components extracted |
| P2: `isSessionLengthPreset` single-use wrapper | Fixed — removed from `TyreWearAnalysisScreen.tsx` |
| P2: `RootTabParamList` navigation types gap | Fixed — `TrackMemoryHub` + `TrackMemory` added to union |
| P2: homeMode flash | Fixed — as above |
| P2: DEM leftover GPX files (Bend GT/International) | Resolved — not present in `scripts/track-memory-gpx/` |
| Android: AAPT mergeReleaseResources PNG/JPEG mismatch | Fixed — real PNGs committed (`ccfe018`) |
| Android: WebP launcher icons wrong format | Fixed — real WebP mipmaps (`3a3dc2f`) |
| Render: AU cache pushes triggering unnecessary rebuilds | Fixed — `[skip render]` + CI path filter (`4df4dc5`) |

---

## Out of scope

- Pixel / visual QA, Maestro / detox E2E.
- iOS / Android simulator testing (no simulator available in cron context).
- App Store / Play Store review status.
- Onboarding facts (`test-onboarding-facts.mjs`) — not run this cycle; no diff on
  `onboardingRiders.json` / `onboardingBikes.json`.
- GPT knowledge pack parity (`GPT_REPO_PARITY_AUDIT_*.md`) — no Coach KB changes this cycle.
