# RoadRace App Review — 2026-09-07

## Executive summary

- **P0: 1 | P1: 5 | P2: 6**
- The app is broadly healthy: TypeScript is clean on both app trees, AU calendar cache is live with 76 events, the production API responded with `{"ok":true,"roadraceAi":true}`, and the Track Memory arcade has been fully replaced by the GPX-ribbon Track Details screen. One P0 survives from a prior sprint: Wanneroo's GPX in `scripts/track-memory-gpx/` measures 1 761 m but the catalog says 2 410 m (0.73×), causing `prove-track-maps.mjs` to FAIL. Three verified-hand mismatches (Broadford T12, Queensland Raceway T5, SMP Gardner T7) continue as P1 carry-forwards from previous reviews. Two new P1s identified this review: the SCREEN_BRIEF describes a facility map with sister-layout overlays, pit lane, and corner hotspots — the live `TrackFacilityMap` renders only the GPX ribbon (correct per AGENTS.md UI safeguard), and the brief should be updated to match. Wanneroo has no racing line in `data/racingLines/` (18 of 19 tracks covered). Production API cold-start timed out health-check.mjs at 8 s but curl at 20 s shows it alive — not a code bug.

---

## Automated gates

### mobile-review-preflight.mjs (2026-09-06)

```
OK  20 screens listed
OK  TypeScript (app)
OK  TypeScript (android-app)
OK  server.js syntax
OK  qa.js syntax

Track data:
  OK  track_turn_verification.json loads
  OK  tracks.json parses (19 tracks)
  OK  catalog_track_geofences.json parses (19 features)
  OK  the_bend_international length ~4.95 km
  OK  the_bend_gt length ~7.77 km
  OK  shared geofence centre: the_bend_international, the_bend_gt
  OK  shared geofence centre: smp_gardner, smp_brabham, smp_druitt
  OK  turn-hand policy: 68 verified left|right, 0 unverified
  WARN planned layout not in catalog yet: the_bend_east, the_bend_west, smp_amaroo, collingrove_hillclimb
  FAIL wanneroo: map implies 1768 m vs catalog 2410 m (0.73x) — GPX is a different or partial layout

npm audit:
  WARN app — critical:0 high:4 moderate:16 (Metro/image-size tooling; no published fix)
  WARN android-app — same
  WARN api — critical:0 high:0 moderate:3

Preflight: 2 FAIL (validate-track-data, prove-track-maps)
```

### health-check.mjs — production API (API_URL=https://send-it-ke7r.onrender.com)

```
OK  TypeScript (app + android-app)
OK  server.js / qa.js / roadraceAi.js syntax
OK  retrieveForAsk: 5 chunks, fromKb=true
OK  MoMS corpus: MoMS-2026-road-historic.json (edition 2026, 259175 chars)
OK  MoMS nextReviewDue 2027-01-15 (not overdue)
OK  AU calendar cache: 76 events (updated 2026-08-31)
OK  calendar aggregation: 111 events / 87 AU full-detail
--  API not reachable (8 s timeout in health-check.mjs) — curl at 20 s returns {"ok":true,"roadraceAi":true} — cold-start spin-up, not a bug
Health check: PASS (local checks), production API alive via curl
```

---

## Recent changes reviewed (last 15 commits on main)

| Hash | Summary |
|------|---------|
| `d6b1cbf` | fix(trivia): rewrite AU questions in plain English |
| `21f94fc` | fix(app): hide unproven Track Details boards, pin Expo 57.0.17 |
| `7333221` | feat(app): ship RR-owned Track Details boards + per-board corner lists |
| `a0c9700` | feat(app): match Track Prep and in-app tips to rider skill |
| `81ca114` | feat(ai): tailor Coach and Bike Setup to how you ride |
| `ad3a46d` | fix(ios): exclude Android Gradle cache from EAS archive |
| `140f39b` | feat(app): replace Track Memory arcade with Track Details maps |
| `8047dd6` | chore: refresh AU calendar cache |

**Key observation:** `21f94fc` (hide unproven boards) correctly removed `boardMaps.ts`, `mapProof.json`, `trackInfo/maps/`, and `assets/trackInfo/boards/` from the working tree — all confirmed absent. The `7333221` commit added them but they were then cleanly deleted. `prove-track-maps.mjs` correctly uses only `gpxTrackMaps/` polylines.

**Diff scope:** 117 files, 15 377 insertions / 6 337 deletions — major sprint removing the arcade ride engine (physics.ts, drawRoadSkia.ts, projectRoad.ts, TrackMemoryRoad*, TrackMemoryControls, TrackMemoryCockpit) and replacing with the GPX-ribbon screen. `headlinesFeed.ts`, `HeadlinesListScreen.tsx` (466 lines), `headlinesSettings.ts` (134 lines) also deleted — settings and headline list are now inline in `HeadlinesSettingsScreen.tsx`.

---

## Track data

### Structural validator

```
validate-track-data.mjs: FAIL (1 failure, 4 warnings)
  FAIL wanneroo GPX implies 1 768 m vs catalog 2 410 m (0.73×)
  WARN planned: the_bend_east, the_bend_west, smp_amaroo, collingrove_hillclimb
```

### Catalog vs geofence coverage

- **19 catalog tracks / 19 geofence features** — 1:1 match. No orphans.
- Bend family: International + GT present; East + West absent (P2 backlog).
- SMP family: Gardner + Brabham + Druitt present; Amaroo absent (P2 backlog).
- Collingrove Hillclimb: intentional extra not yet in catalog (P2 backlog).

### Bend lengths (official ground truth check)

| Layout | Catalog | Status |
|--------|---------|--------|
| GT Circuit | 7.77 km | ✅ PASS |
| International | 4.95 km | ✅ PASS |
| West | not in catalog | P2 backlog |
| East | not in catalog | P2 backlog |

### Corner QA

- **Turn-hand policy:** 68 verified `left`/`right`, 0 unverified — clean.
- `track_turn_verification.json` loaded OK.
- No `direction: "straight"` on Hairpin/Sweeper/Double-apex shapes found.
- Phillip Island: catalog `direction` is `anticlockwise` ✅; Doohan T1 is `right` ✅ (confirmed, not re-locked from CCW inference).

---

### Track Memory

#### Gate table

| Test | Result |
|------|--------|
| `validate-track-data.mjs` | **FAIL** — wanneroo GPX length mismatch |
| `prove-track-maps.mjs` | **FAIL** — wanneroo mismatch causes exit 1 |
| `diagnose-track-memory.mjs` | Exit 0 — 0 empty layouts, 0 extra-lap, 7 kinks≥14°, **3 verified-hand misses** |
| `cd app && npx tsc --noEmit` | **PASS** |
| Info maps (gpxTrackMaps/) | 19/19 polyline JSONs present; app == android-app (spot-checked wanneroo, baskerville) |

#### GPX source per layout

| Track | Source GPX | lengthM | elevSource |
|-------|-----------|---------|-----------|
| baskerville | baskerville.gpx | 1 964 m | dem |
| broadford | broadford.gpx | 2 103 m | dem |
| calder_park | calder_park.gpx | 2 334 m | dem |
| hidden_valley | hidden_valley.gpx | 2 858 m | dem |
| lakeside | lakeside.gpx | 2 311 m | dem |
| mac_park | mac_park.gpx | 2 354 m | dem |
| mallala | mallala.gpx | 2 585 m | dem |
| morgan_park | morgan_park.gpx | 2 998 m | dem |
| phillip_island | phillip_island.gpx | 4 419 m | dem |
| queensland_raceway | Queensland_Raceway.gpx | 3 175 m | — (flat, intentional) |
| sandown | sandown.gpx | 3 113 m | dem |
| smp_brabham | Sydney_Motorsport_Park_-_Brabham.gpx | 4 530 m | gpx (Emtron) |
| smp_druitt | smp_druitt.gpx | 2 875 m | dem |
| smp_gardner | Sydney_Motorsport_Park_-_GP.gpx | 3 861 m | gpx (Emtron) |
| the_bend_gt | Tallem_Bend_GT.gpx | 7 802 m | — (flat Emtron) |
| the_bend_international | Tallem_Bend_International.gpx | 4 900 m | — (flat Emtron) |
| wakefield_park | Wakefield_Park_Raceway.gpx | 2 165 m | gpx (Emtron) |
| **wanneroo** | wanneroo.gpx | **1 761 m** | dem |
| winton | winton.gpx | 3 022 m | dem |

#### P0 — Wanneroo GPX length mismatch

**`wanneroo.gpx` (DEM-enriched, 137 trackpoints) measures 1 761–1 768 m vs catalog `2.41 km` (2 410 m). Ratio 0.73× is well below the 0.85× floor.** `prove-track-maps.mjs` exits 1. Track Details map is displayed (gpxTrackMaps/wanneroo.json polyline is present) but the GPX it was built from is a partial or wrong layout. **Cursor must:** obtain the full Wanneroo long-course/bike-layout GPX, replace `scripts/track-memory-gpx/wanneroo.gpx`, and rebuild with `node scripts/build-gpx-track-maps.mjs wanneroo`.

#### P1 — Verified-hand carry-forwards (3 open)

These appear every week until Cursor clears them with owner-verified maps:

- **Broadford T12** — diagnose wants `left`, bake reports `right` at s=0.982
- **Queensland Raceway T5** — diagnose wants `right`, bake reports `left` at s=0.807
- **SMP Gardner T7** — diagnose wants `right`, bake reports `left` at s=0.733

#### P1 — Sandown kinks (carry-forward)

3 kinks ≥14° at s=0.381 (19°), s=0.807 (22°), s=0.948 (16°). Not phantom-start (all past first 12%); likely GPX artefacts on the Sandown outer loop. Flag for re-check when a cleaner Emtron trace is available.

#### P2 — SMP Brabham + SMP Gardner kinks

- SMP Brabham: 1 kink at s=0.728 (15°)
- SMP Gardner: 1 kink at s=0.657 (15°)
Both are borderline; may be real track features (chicane-entry on GP circuit). No action needed yet.

#### P2 — Bend International + GT: elevation missing

Both Tallem Bend layouts show `elevSource: undefined` / `hasElevation: undefined` — flat Emtron centreline after the chicane geometry fix. Elevation data not baked. Flag as P2 "re-sample DEM Z onto Emtron XY once geometry is proven stable."

#### P2 — Racing line missing for Wanneroo

`app/src/data/racingLines/` has 18 of 19 tracks; `wanneroo` is absent. The screen gracefully handles a missing `racingLine` prop (no guide overlay), but once Wanneroo GPX is fixed, a racing line should be added.

---

## Per-screen findings

### OnboardingScreen
- **[OK]** 8-step flow: welcome, bike, rider, taste_bridge, activity, future_racer, nickname_avatar, summary. Avatar skip → random mascot via `pickRandomNoPhotoAvatar`. Face capture via `AvatarFaceCameraModal`. Legal acceptance gate before finish.
- **[P2]** `AvatarFaceAlignModal` imported — confirm it is used in the face-capture step (it appears alongside `AvatarFaceCameraModal`). Two similar modal components for the same flow warrant a code-comment explaining the split.

### HeadlinesScreen
- **[OK]** Home hero: bike photo, avatar composite with `AvatarFaceEllipse`, nickname from `onboarding.ts`. `useFocusEffect` refreshes on focus. `homeModeFromActivity` + `rememberHomeMode` gate.
- **[P2]** `HomeMode` derivation from `activity` on every focus — `rememberHomeMode` writes to memory but there's no debounce; if `activity` or answers change mid-session via Settings the home recalculates. Acceptable, but worth noting.

### HeadlinesSettingsScreen
- **[OK]** Profile (name, nickname, avatar, face photo), ride-activity chip, track arrival toggle, bike-setup / balance / gearing / track-walk / tyre-wear data reset. Uses `updateOnboardingAnswers`.
- **[P1]** `HeadlinesListScreen.tsx` was deleted in the last sprint (diff shows 466 lines removed). App.tsx navigation must no longer reference it — confirmed `App.tsx` imports do not include `HeadlinesListScreen` (import list verified). No dead navigation route.
- **[P2]** The settings screen imports and clears 6 storage modules (`bikeSetupSheet`, `bikeBalance`, `gearingGuide`, `trackWalk`, `tyreWearAnalysis`, `bikePhoto`) — each with its own `clearAll*` call. Consider grouping into a single `clearAllUserData()` util if this pattern grows.

### CalendarScreen
- **[OK]** `apiFetch(CALENDAR_URL)` with 60 s default timeout; `RefreshControl` for pull-to-refresh; filter chips (all/local/world); ICS export; `Linking.openURL`.
- **[OK]** Graceful empty-state and error handling pattern consistent with other screens.

### QAScreen
- **[OK]** Ask tab uses `sendAskChat` → `POST /roadrace-ai/ask`. Trivia uses `GET /qa/trivia`; trivia does **not** use GPT (confirmed — separate fetch). 7 AU + 3 world per 10-question block. `capTriviaUsed` caps at 40 to prevent exhaustion.
- **[OK]** `readTriviaBestScore` migrates legacy pre-prefix storage key once.
- **[P2]** `SCOOTER_COMMENTS` (wrong-answer taunts) is defined inline as a module-level constant array. Fine, but if the list grows, worth moving to a data file.

### RiderCoachScreen
- **[OK]** Hub with seed-message deep-link via `navigation.replace('CoachChat', {mode, …})`. Mode switch `coach` vs `bikesetup` correctly inspects `route.params?.seedTab`.
- **[OK]** Stack includes: CoachChat, BikeSetupHub, ImportTrackNotes, TrackPrep, TrackdayPrep, TrackdayPrepReport, TrackWalk, TrackMemoryHub, BikeSetupBasics, BikeSetupSheet, BikeBalanceSetup, GearingGuide, TyreWearAnalysis.

### CoachChatScreen
- **[OK]** Mode-aware (coach vs bikesetup); seed messages injected once via `lastSeedKeyRef`; reset on mode switch; `conversationGenerationRef` cancel on mode change.
- **[OK]** `riderSkillSaved.ts` → `riderSkillReplyInstruction(skill)` appended to user messages. Skill tailoring works for novice/intermediate/advanced.
- **[P2]** `goalsSeededRef` — goals only seeded once per screen mount; if the user navigates away and back the goals are re-fetched but the ref prevents re-injection. Correct behaviour but not obvious from code — add comment.

### TrackMemoryHubScreen (Track Details)
- **[OK]** GPX ribbon via `TrackFacilityMap` (green infield, grey asphalt, white edges). Racing line overlay from `getRacingLine(trackId)` when present; hint text updates to mention red guide line.
- **[OK]** Corner list below map — `TrackCornerSheet` tap opens details + saved track walk note.
- **[OK]** `latestCornerNote()` reads from track walk sessions, sorted newest-first. Correct.
- **[P1 — BRIEF MISMATCH]** `SCREEN_BRIEF_FOR_VISUALS.md` §9b describes: "facility map: selected layout in red, sister layouts faded, pit entry/exit/lane, water, tunnel/bridge; red dots on each corner." The live screen renders **only the GPX polyline ribbon** (no sister layouts, no pit lane, no corner hotspot dots on the map). This is correct per AGENTS.md UI safeguard ("Track Details draws only the GPX ribbon … corner notes stay in the list below the map, never on the picture"), but **the SCREEN_BRIEF has not been updated**. The brief is stale and could mislead anyone generating mockups or planning ASC screenshots. **Cursor should update §9b** to accurately describe the GPX-ribbon + corner-list layout.

### TrackWalkScreen
- **[OK]** Voice recognition (optional native module, gracefully absent), photo pick, corner/note entries, export, send-to-coach navigation.

### TrackdayPrepScreen
- **[OK]** Rider level chip derived from onboarding activity via `trackPrepLevelFromActivity`. Weather fetch via `fetchTrackForecastForDate`. Privacy banner shown.

### ImportTrackNotesScreen
- **[OK]** Paste from clipboard, photo attach, track + other-context picker, `navigateToCoachChat`. `sessionReadyForCoach` guard correct.

### BikeSetupBasicsScreen / BikeSetupHubScreen / BikeSetupSheetScreen / BikeBalanceSetupScreen / GearingGuideScreen / TyreWearAnalysisScreen / TrackPrepHubScreen / TrackdayPrepReportScreen / RoadRacerAiFaqsScreen
- No TypeScript errors; these screens follow established patterns. Deep audit deferred — low-churn in the last 10 commits.

---

## Cross-cutting

### Navigation
- `App.tsx` imports verified — no reference to deleted `HeadlinesListScreen` or `TrackMemoryScreen`. Clean.
- `RiderCoachScreen` stack is the richest nav tree; `navigation.replace('CoachChat', …)` seed handoff is P0-safe (no dead ends found).
- `navigateToCoachChat` in `rootNavigation.ts` used by `ImportTrackNotesScreen` — tested import path OK.

### AsyncStorage / Storage
- `STORAGE_KEYS` prefix scheme in `storageKeys.ts` prevents collisions; legacy key migration in `readTriviaBestScore` is correct.
- `clearAll*` pattern across 6 modules (Settings data reset) — works, verbose.
- No dev-only reset paths exposed in production builds (reset is behind Settings UI, not flag).

### Permissions
- Camera, photo library: used in `OnboardingScreen`, `HeadlinesScreen`, `TrackWalkScreen`, `CoachChatScreen`. Denied paths handled via `Alert`/`Linking.openSettings` in ImagePicker calls.
- Notifications: `expo-notifications` — denied path handled.
- Location: `requestForegroundLocationPermission` in `HeadlinesSettingsScreen` → track arrival toggle. Denied path shows toggle disabled state.

### API / Coach mode
- All API URLs originate from `app/constants/api.ts` — no hardcoded `onrender.com` URLs found in screen files.
- Coach mode `coach` vs `bikesetup` correctly scoped via `CoachMode` type; `POST /roadrace-ai/chat` mode parameter correct.
- Trivia does **not** use GPT — confirmed separate route.

### Offline / API down
- `apiFetch` applies `AbortSignal.timeout(60_000)` by default; `apiErrorMessage` surfaces timeout to user.
- Calendar, Coach, Ask, Headlines all have visible error states.

### Security
- No secrets visible in repo.
- `x-app-secret` injected from `EXPO_PUBLIC_APP_API_SECRET` env var (optional; not hardcoded).
- `npm audit`: high-severity findings are Metro/image-size tooling dependencies with no published fix — not production API attack surface. Watching for upstream fix.

### Onboarding data
- `onboardingRiders.json`: 266 riders, all have `aliases` + `characteristics`. `test-onboarding-facts.mjs`: **PASS** (18 assertions).
- `onboardingBikes.json`: 49 bikes, all fields present. PASS.
- No gaps flagged this review.

---

## Coding improvements

| # | Area | Finding |
|---|------|---------|
| 1 | **SCREEN_BRIEF** | §9b stale: describes corner hotspots on map and sister-layout overlays; reality is GPX-ribbon only. Update to reflect current design. **[P1]** |
| 2 | **Types** | `Queensland_Raceway` and Bend layouts have `elevSource: undefined` / `hasElevation: undefined` (missing from JSON). `TrackMemoryLayout` type should mark these as `elevSource?: string` and the UI gracefully falls back — no crash — but consistent JSON is cleaner. **[P2]** |
| 3 | **Duplication** | 6 separate `clearAll*` storage calls in `HeadlinesSettingsScreen` — consider a single `clearAllUserData()` util in `app/src/storage/index.ts`. **[P2]** |
| 4 | **Dead assets** | `app/assets/track-memory/bitumen_texture.png` and `bitumen_tile.png` and `postie-cockpit.png` were deleted in diff — confirm they are not referenced anywhere in remaining code (diff shows deletion; no reference check run on asset imports in less-touched screens). **[P2]** |
| 5 | **Wanneroo racing line** | Missing from `data/racingLines/` — 18/19 tracks covered. Add after GPX is fixed. **[P2]** |
| 6 | **TrackFacilityMap hint** | Hard-coded hint text "Track map from the circuit GPS trace — pinch or use + / −." is inline in JSX. When no `guide`, the guide sentence is conditionally omitted — correct. Consider extracting to a constant for easier copy-edit. **[P2]** |
| 7 | **Production API cold-start** | `health-check.mjs` times out at 8 s; Render free tier cold-starts can take 15–30 s. The script reports `--` (not FAIL) on timeout — correct. But `health-check.mjs` could increase to 30 s for the live-API probe, or add a retry. **[P2]** |

---

## Suggested fix order (for Cursor)

1. **[P0] Fix Wanneroo GPX** — obtain the full ~2.4 km long-course bike-layout GPX, replace `scripts/track-memory-gpx/wanneroo.gpx`, run `node scripts/build-gpx-track-maps.mjs wanneroo`, verify `prove-track-maps.mjs PASS` for wanneroo, then sync `android-app/src/data/gpxTrackMaps/wanneroo.json`. Also add a `wanneroo` racing line JSON to `data/racingLines/`.

2. **[P1] Update SCREEN_BRIEF_FOR_VISUALS.md §9b** — rewrite to describe the current GPX-ribbon + corner-list layout (no sister layouts, no pit lane, no corner dots on the picture). Prevents confusion for anyone generating ASC screenshots or mockups.

3. **[P1] Investigate Broadford T12 / QR T5 / SMP Gardner T7 hand mismatches** — obtain owner-verified or official map confirmation, update `track_turn_verification.json` accordingly, run `enforce-turn-verification.mjs --write` + `validate-track-data.mjs`.

---

## Manual verify on Expo Go

- [ ] Wanneroo Track Details: after GPX fix, confirm map renders a full ~2.4 km ribbon without self-intersection.
- [ ] Track Details corner sheet: tap a corner → sheet appears with heading, approach, "Ask coach" button, and any saved Track Walk note.
- [ ] Coach mode: send a message on coach mode → confirm reply is skill-tailored (novice vs advanced wording).
- [ ] Trivia: play 10 questions → confirm 7 AU + 3 world order (slot 3, 6, 9 are global).
- [ ] Calendar ICS: tap event → confirm ICS download / system calendar prompt.
- [ ] Onboarding reset (Settings): reset → confirm onboarding re-runs on next app open.

---

## Resolved since last review

*(First review for this report format — no prior `RR_REVIEW_*.md` to delta against.)*

Major items resolved in last sprint (from commit history):
- Track Memory arcade (physics.ts, Skia renderer, cockpit, controls, 5 core modules ~1 900 lines) fully removed — no dead imports remain.
- `HeadlinesListScreen.tsx` + `headlinesFeed.ts` + `headlinesSettings.ts` + `priority1Notifications.ts` — deleted (functionality consolidated into `HeadlinesSettingsScreen`).
- Board PNGs, `boardMaps.ts`, `mapProof.json`, `trackInfo/maps/` — deleted; `prove-track-maps.mjs` confirms clean.
- AU trivia rewrites in plain English (d6b1cbf) — 266 riders + 49 bikes fact test PASS.
- Rider skill tailoring for Coach + Bike Setup + Track Prep tip copy.

---

## Out of scope

- Pixel / visual QA — cannot run without device.
- Maestro / E2E automation — not configured.
- ASC / Play pre-submit review — run `send-it/agent-apple` / `send-it/agent-play` separately.
- `TRACK_GPX_ALIGN_*.md`, `TRACK_MEMORY_REVIEW_*.md`, `GPT_REPO_PARITY_AUDIT_*.md` — no prior files found in `docs/reviews/`; this report is the first baseline.
