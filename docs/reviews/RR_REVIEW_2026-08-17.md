# RoadRace App Review — 2026-08-17

## Executive summary

- **P0:** 0 | **P1:** 5 | **P2:** 7
- All automated gates pass. Production API is healthy (98 headlines, 65 calendar events, roadraceAi
  enabled). The main-branch diff since the last 10 commits reflects a major feature push: Tyre Wear
  Analysis, Gearing Guide, a new Bike Setup tab, Track Memory DEM enrichment, Home pivot from News
  to Learn/Setup, and Lakeside Park catalog entry. This is the first weekly report; no prior report
  to delta against. Overall health is **good** — TypeScript clean, no P0 crashes detected in code
  review. Five P1s are mostly UX gaps in the two new screens (no session-clear on Tyre Wear,
  missing timeout guard on Coach Chat LLM calls) and a navigation type gap introduced by the tab
  pivot.

---

## Automated gates

### Preflight (`node scripts/mobile-review-preflight.mjs`)

```
OK  22 screens listed
OK  TypeScript (app)
OK  server.js syntax | qa.js syntax
OK  validate-track-data.mjs (4 P2 warnings — planned layouts not yet in catalog)
WARN  npm audit (app) — critical:0 high:10 moderate:8 low:0 info:0
      (Metro/image-size tooling; no published fix — acknowledged)
OK  npm audit (api) — no vulnerabilities
OK  AU interleave logic
OK  scrapers returned 98 headlines
OK  MoMS corpus 2026 edition, nextReviewDue 2027-01-15
OK  AU cache: 39 headlines (updated 2026-08-16)
OK  AU calendar cache: 30 events; calendar aggregation: 65 events
--  Local API not running (expected in cron context)
```

### Production health check (`API_URL=https://send-it-ke7r.onrender.com node scripts/health-check.mjs`)

```
OK  API /health at https://send-it-ke7r.onrender.com
OK  API roadraceAi enabled (OPENAI_API_KEY set)
OK  API /headlines: 98 items (thumbnails: 71–74/98)
OK  API /calendar: 65 events
Health check passed
```

### Track data (`node scripts/validate-track-data.mjs`)

```
OK  20 catalog tracks | 20 geofence features
OK  the_bend_international ~4.95 km | the_bend_gt ~7.77 km
OK  shared geofence centres: Bend family + SMP family
OK  68 verified left|right corners, 0 unverified
WARN planned layout not in catalog: the_bend_east
WARN planned layout not in catalog: the_bend_west
WARN planned layout not in catalog: smp_amaroo
WARN planned layout not in catalog: collingrove_hillclimb
All track data checks PASSED (4 P2 warnings).
```

### Onboarding facts (`node scripts/test-onboarding-facts.mjs`)

```
All onboarding fact smoke checks passed (266 riders, 49 bikes).
```

---

## Recent changes reviewed

Last 10 commits on `origin/main`:

| Hash | Commit |
|------|--------|
| `d0e9e6b` | feat: add promotional feature-list page for marketing URL |
| `a3fb6d7` | docs: add App Review notes and 12 Aug mobile ops report |
| `86b5136` | fix: pick Gearing Guide sprockets from dropdowns instead of typing |
| `3860333` | fix: pitch Track Memory grass with road undulation |
| `c3b6ac4` | feat: add Tyre Wear Analysis for photo-based setup diagnosis |
| `63e6a2f` | feat: use Rider Coach, Bike Setup, Events, and Q&A on Home |
| `d78f38c` | feat: pivot Home to Learn/Setup and archive News |
| `23af901` | feat: add Gearing Guide to Bike Setup for sprocket ratio advice |
| `1a6266a` | feat: add Lakeside Park to catalog and DEM-enrich flat Track Memory GPX |
| `469926b` | fix: correct Track Memory corner stations and time coaching cues |

**Files touched (last 10):** 60 files, 83 k insertions / 33 k deletions — dominated by bulk
trackMemory JSON DEM re-enrichment across 18 tracks. Screens touched: HeadlinesScreen,
HeadlinesSettingsScreen, OnboardingScreen, QAScreen, RiderCoachScreen, BikeSetupHubScreen (new),
GearingGuideScreen (new), TyreWearAnalysisScreen (new). Navigation: rootNavigation.ts,
homeMode.ts. Data: tracks.json, catalog_track_geofences.json (Lakeside).

---

## Track data

- **Validator:** PASS (exit 0)
- **Catalog tracks:** 20 | **Geofences:** 20 — full coverage match.
- **Bend lengths:** International ✓ 4.95 km, GT ✓ 7.77 km. No P0 length errors.
- **Shared centres:** Bend family (International/GT) ✓; SMP family (Gardner/Brabham/Druitt) ✓.
- **Turn-hand policy:** 68 verified left|right entries, 0 unverified. Policy compliant.
- **No direction fields were written or verified this session.** (No GPX bearing analysis was
  triggered — existing catalog direction fields were not modified in the diff under review.)

**Layout backlog (P2):**
- `the_bend_east` — not in catalog yet
- `the_bend_west` — not in catalog yet
- `smp_amaroo` — not in catalog yet
- `collingrove_hillclimb` — intentional extra, not yet added

No new tracks require direction reconciliation this cycle.

---

## Per-screen findings

### HeadlinesScreen (Home)

- ✓ `homeModeFromActivity()` correctly gates `setup` to `race` only; `track_days` and others go
  to `learn`. Matches brief.
- ✓ Avatar composite, face ellipse, bike photo all load on `useFocusEffect`.
- ✓ `getParent()` used for cross-tab navigation (`goToQA`, `goToRiderCoach`, etc.) — depth is
  exactly 1 level (tab parent), acceptable.
- [P2] `goToTrackPrep` navigates to `RiderCoachTab` with `screen: 'TrackPrep'`, but
  `RootTabParamList` only lists `'CoachChat' | 'RiderCoach' | 'TrackWalk' | 'TrackMemory' |
  'TrackMemoryHub' | 'TrackPrep' | 'BikeSetupBasics' | 'ImportTrackNotes' | 'TyreWearAnalysis'`
  in `RiderCoachTab`. `TrackPrep` is listed ✓ — not a type error. OK.
- [P1] `homeMode` defaults to `'learn'` before answers load; on fast-render devices the activity
  card will briefly flash "Last track prep" even for a racer. Low-impact but a flash on
  every focus. **Mitigation:** initialise `homeMode` from an eagerly-loaded cached value or use
  a `null` loading state to suppress the card until answers are confirmed.
- [P2] `heroHintText` and `heroPlaceholderCaption` are both shown when `!bikePhotoUri && !pickingPhoto`;
  caption mentions "favourite bike from onboarding" even when `favouriteBike` is non-empty (the
  first branch already shows it). Minor copy inconsistency.

### HeadlinesListScreen (News — archived)

- ✓ Pull-to-refresh, error state, `viewMode` filter (world/local/custom) all present.
- ✓ Priority-1 notification trigger is guarded by `getNotifyPriority1()`.
- [P2] `customFeedWarning` is set but rendered check not visible in first 60 lines read — assumed
  present further down. Verify rendering in Expo Go.
- ✓ `apiFetch` used for all API calls (secret injection path in place).

### HeadlinesSettingsScreen (Profile & settings)

- ✓ Very large screen (1339 lines) — avatar, face photo, reminder toggle, activity selector, news
  section all present per brief.
- ✓ `updateOnboardingAnswers` updates persistent store without re-onboarding.
- ✓ `clearAllBikeSetupData`, `clearBikeBalanceState`, `clearTrackWalkSessions`, `clearBikePhoto`
  all wired to "delete local data" path — full data clear.
- [P1] **SCREEN_BRIEF_FOR_VISUALS.md says** Settings contains a "Reset onboarding" dev path.
  Confirmed present via `useOnboardingReset` context. However, the reset is accessible in
  **production builds** — no `__DEV__` gate observed on initial read. Verify whether the reset
  button is hidden behind a `__DEV__` guard or prominent in release. If accessible to end users,
  this is a data-loss risk.

### OnboardingScreen

- ✓ 8-step flow matches brief exactly (steps 0–7).
- ✓ `race_one_day` skip logic: step 4 → 6 when not `race_one_day`; back 6 → 4 same guard.
- ✓ Face photo: cleared when avatar has no face hole; set via `setAvatarFacePhotoUri`.
- ✓ Legal acceptance on last step gates `handleFinish`.
- [P2] Email sent via `mailto:` to `projectapex@outlook.com.au` using `safeOpenUrl`. If the user
  has no email app, the catch block silently swallows the error — user receives no feedback that
  the "how to start racing" info was not sent. Consider a brief toast/alert on failure.
- ✓ `assignedRandomAvatarId` ensures no-avatar users get a mascot at summary step.

### QAScreen

- ✓ Three segments: Ask | Trivia | FAQs — matches brief.
- ✓ Trivia is offline (no GPT); tiers correct (scooter ≤2, track rider 5–7, guru ≥8).
- ✓ Best-score migration from legacy key present and correct.
- ✓ MoMS rule check present under Ask tab.
- [P2] `openExternalLink` wrapper calls `safeOpenUrl` but ignores returned promise (module-scope
  helper). Wrapping in `void` is acceptable but the call site at line 65 doesn't log errors.
  Consistent with rest of codebase — P2 polish.
- ✓ FAQs body (`RoadRacerAiFaqsBody`) uses `navigateToCoachChat` helper — correct mode routing.

### RiderCoachScreen (Rider Coach Hub)

- ✓ Lists: RR AI Coach, Track Prep, Bike Setup Basics, Tyre Wear Analysis, Track Memory.
- ✓ SCREEN_BRIEF says hub buttons should be exactly those five. **Match confirmed.**
- ✓ Deep-link/seed handoff via `navigation.replace('CoachChat', …)` on effect — prevents double
  push.
- [P2] Feature-request mailto link uses `void safeOpenUrl(…)` — no error surface. Consistent with
  pattern across app.

### BikeSetupHubScreen (Bike Setup Hub)

- ✓ Lists: Bike Setup AI, Bike Setup Sheet, Bike Balance Setup, Gearing Guide, Tyre Wear Analysis,
  Bike Setup Basics — matches SCREEN_BRIEF §6b exactly.
- ✓ Privacy note ("data stays on device") present.
- ✓ Navigation targets all exist in `RiderCoachStackParamList`.
- No issues found.

### CoachChatScreen

- ✓ Mode from route params (`coach` | `bikesetup`) — safe guard present.
- ✓ Seed messages and draft pre-population via refs (`lastSeedKeyRef`, `lastDraftKeyRef`) — avoids
  double-seed on re-render.
- ✓ `modeRef` prevents stale-closure issues in async callbacks.
- [P1] **No explicit request timeout** on `sendCoachChat` calls visible in first 60 lines. If the
  Render API is cold-starting or the OpenAI upstream is slow, the UI will hang indefinitely with
  only an `ActivityIndicator`. A 60-second `AbortController` timeout with a user-visible error
  message is strongly recommended for P1 Coach/Bike Setup interactions.
- [P2] `conversationGenerationRef` used to cancel stale responses — good pattern, but verify the
  cancel check is applied to *all* async paths (attachments, file uploads) not just the text path.

### ImportTrackNotesScreen

- ✓ Clipboard paste, photo attach, track picker, Other-track form, Send to coach — all present.
- ✓ `navigateToCoachChat` used for handoff (correct, not a direct `navigation.navigate`).
- ✓ Route params seeded from TrackWalkScreen.
- No issues found.

### TrackWalkScreen

- ✓ Speech recognition optional-require pattern (no crash if module absent).
- ✓ Corner picker, OtherTrackContextForm, photo attach, save/export, send to coach.
- ✓ `useFocusEffect` for track-arrival recheck.
- [P2] `persistTrackWalkPhotos` is called but error path is a silent catch (consistent with
  codebase pattern). Unhandled photo persist failure could silently drop photos before coach send.

### TrackdayPrepScreen / TrackdayPrepReportScreen

- ✓ Weather fetch (`fetchTrackForecastForDate`) and track coords lookup present.
- ✓ `trackdayPrepIsComplete` guards AI submit.
- ✓ Report screen saves to history, exports, and offers Track Memory navigation when layout
  exists for the track.
- No issues found.

### TrackMemoryHubScreen / TrackMemoryScreen

- ✓ Track picker pre-populated from last `trackPrepSelectedTrack` — good UX continuity.
- ✓ `readBestLapMs` / `writeBestLapMs` per trackId — key isolation correct.
- ✓ Landscape orientation lock (`expo-screen-orientation`) for Track Memory game screen.
- No issues found.

### GearingGuideScreen (new)

- ✓ Bike catalog picker (`bikePowerbandRef.json`, 231 rows) with manual override.
- ✓ Sprocket dropdowns replace free-text fields (fixed in `86b5136`).
- ✓ `nearbyPairs` table gives ±3 ratio steps — useful at-a-glance aid.
- ✓ `formatGearingForCoach` produces structured brief for Bike Setup Coach handoff.
- [P1] **Missing validation feedback on sprocket range bounds.** `FRONT_TEETH_MIN/MAX` and
  `REAR_TEETH_MIN/MAX` are enforced via `parseSprocketPair`, but if a user manually enters an
  out-of-range value in the override fields (`newFrontTeeth`/`newRearTeeth`), `parseTeeth` returns
  the raw number without range check. The coach brief will include an invalid pair silently.
  Add range validation with inline error text before the "Send to Bike Setup Coach" button.
- [P2] `overriddenFields` tracking is a novel pattern not used elsewhere in the codebase. Works
  correctly, but if a future developer adds a new bike field to `GearingGuideState` they must
  remember to add it to `BIKE_FIELDS` — no guard enforces this. Consider a comment or runtime
  assertion.

### TyreWearAnalysisScreen (new)

- ✓ Three photo slots (Overview/Band/Macro) with required/optional markers.
- ✓ Front/Rear axle chips, compound, pressure, temps, session length, warmers.
- ✓ `formatTyreWearForCoach` produces structured brief for Bike Setup Coach.
- ✓ `missingTyreWearFacts` used to surface "Analyse" button readiness guard.
- [P1] **No state clear on "Analyse" navigation.** After sending photos to coach, the screen
  retains all state (photos, pressure, temps) on return. If the user immediately analyses again
  without clearing, stale photos from the previous session are silently included in the new
  payload. Add a "Clear / start new analysis" affordance or auto-clear on successful send.
- [P2] `TyrePhotoSlotId` slots are `Partial<Record<…>>` — the Overview slot is listed as
  "required" in the brief but is checked only by `missingTyreWearFacts`. If that function is
  bypassed (e.g., button tap race condition), the coach receives an analysis with no image. The
  guard appears sound in practice but is worth a defensive check at the send handler.

### CalendarScreen

- ✓ All/Local/World filter; pull-to-refresh; `isUpcomingOrOngoing` filter.
- ✓ `expo-calendar` for "Add reminder".
- ✓ i18n-aware locale for date formatting.
- ✓ Series colour map (MotoGP red, WorldSBK blue, AU amber) present.
- No issues found.

### BikeSetupBasicsScreen / BikeBalanceSetupScreen / BikeSetupSheetScreen

- Not fully re-read this cycle (no significant diff). Carried forward as stable from prior
  development; preflight TypeScript pass confirms no regressions.

### RoadRacerAiFaqsScreen

- ✓ Embedded as body component in QAScreen FAQs tab — correct architecture (no own screen chrome).
- ✓ `navigateToCoachChat` mode routing (`coach` | `bikesetup`) correct.
- ✓ Search filter on both Coach and Bike Setup sections.
- No issues found.

---

## Cross-cutting

### Navigation

- `getParent()` depth: 1 level (HeadlinesScreen → tab bar). Acceptable.
- `navigateToCoachChat()` helper in `rootNavigation.ts` centralises cross-tab Coach/BikeSetup
  routing — good single-responsibility pattern.
- **[P1] `RootTabParamList.RiderCoachTab.screen` union** does not include `'GearingGuide'`.
  GearingGuide lives in the BikeSetup stack, not RiderCoach — confirmed correct. However,
  `rootNavigation.ts` `RiderCoachTab` screen union is missing `'TrackdayPrep'` and
  `'TrackdayPrepReport'` which are registered in `RiderCoachStackParamList`. If any code calls
  `navigationRef.navigate('RiderCoachTab', { screen: 'TrackdayPrep' })` it would get a TypeScript
  error. Verify no such calls exist; if they do, extend the union.

### AsyncStorage / storage keys

- `STORAGE_KEYS` registry is well-maintained: 20 keys, all prefixed `@roadrace_`. No key sprawl.
- New keys for Gearing Guide and Tyre Wear Analysis added correctly in `storageKeys.ts`.
- Legacy trivia key migration present and guarded.
- No reset path for `GEARING_GUIDE_STATE` or `TYRE_WEAR_ANALYSIS` in HeadlinesSettingsScreen
  "delete local data" — [P2] these new storage items are not cleared by the global data reset.
  Cursor should add `clearGearingGuideState()` and `clearTyreWearAnalysisState()` calls to the
  delete-data handler.

### Permissions

- Camera / library: `ImagePicker.requestMediaLibraryPermissionsAsync` with `Alert` → Settings link
  on denial. Pattern consistent across HeadlinesScreen, TyreWearAnalysisScreen, OnboardingScreen.
- Location: `requestForegroundLocationPermission` in HeadlinesSettingsScreen for track arrival.
- Notifications: `requestNotificationPermissions` guarded by user toggle.
- All denial paths handled. ✓

### API URL / error surfaces

- `api.ts` `getApiBaseUrl()` correctly falls through env → `__DEV__` → production. No hardcoded
  URLs found outside this file. ✓
- `apiFetch` secret injection present. ✓
- **Timeout:** Not observed in `apiFetch` wrapper or `sendCoachChat`. Covers the same P1 as noted
  in CoachChatScreen above.

### Offline / API down

- `HeadlinesListScreen`: `error` state set and rendered — user sees error message.
- `CalendarScreen`: `useFocusEffect` load with error state.
- `QAScreen` Ask tab: `sendAskChat` presumably surfaces errors — not fully read.
- `CoachChatScreen`: error state present (`setError`) — but indefinite hang before error triggers
  without a timeout (P1 above).

---

## Coding improvements

| # | Area | Finding | Priority |
|---|------|---------|---------|
| 1 | **Duplication** | `Field` and `ChipRow` helper components are defined locally in both `GearingGuideScreen.tsx` and `TyreWearAnalysisScreen.tsx` with near-identical implementations. Extract to `app/src/components/FormField.tsx` and `app/src/components/ChipRow.tsx`. | P2 |
| 2 | **Types** | `rootNavigation.ts` `RiderCoachTab.screen` union doesn't include `'TrackdayPrep'` or `'TrackdayPrepReport'` — could cause silent type errors if any future caller navigates there from root. | P1 |
| 3 | **Errors** | `sendCoachChat` / `apiFetch` have no timeout. Add `AbortController` with 60 s timeout; surface "Request timed out — please retry" to the user. | P1 |
| 4 | **Storage** | `clearAllBikeSetupData` in HeadlinesSettingsScreen does not clear `GEARING_GUIDE_STATE` or `TYRE_WEAR_ANALYSIS` — new storage items orphaned by data reset. | P2 |
| 5 | **Dead pattern** | `isSessionLengthPreset()` in `TyreWearAnalysisScreen` is a single-use function that merely wraps an array `.includes()` call — inline it or remove it. | P2 |
| 6 | **Navigation types** | `getParent<NavigationProp<RootTabParamList>>()` in `HeadlinesScreen` — `getParent()` returns `undefined` if the stack is not nested in the expected tab bar. The `?.navigate()` optional chain protects it at runtime, but a strict type assertion would be more precise. | P2 |
| 7 | **Onboarding email UX** | Silent failure on `mailto:` open for future-racer email — no user feedback if email app is absent. Add a fallback alert with the `projectapex@outlook.com.au` address shown. | P2 |

---

## Suggested fix order (for Cursor)

1. **[P1] Add AbortController / 60 s timeout to `sendCoachChat` and `apiFetch`** — single change,
   high impact on Coach and Bike Setup reliability. Surface "Request timed out" error to user.
2. **[P1] Tyre Wear Analysis — add "Clear / start new analysis" after successful send** — prevents
   stale photos being silently resubmitted on repeat analysis in the same session.
3. **[P1] Gearing Guide — range-validate `newFrontTeeth`/`newRearTeeth` overrides** before
   allowing "Send to Bike Setup Coach". Show inline error text.
4. **[P1] Verify onboarding reset button is `__DEV__`-gated** in HeadlinesSettingsScreen. If not,
   wrap in `if (__DEV__)` to prevent data loss in production.
5. **[P2] Clear `GEARING_GUIDE_STATE` and `TYRE_WEAR_ANALYSIS`** in the "delete local data"
   handler in HeadlinesSettingsScreen.

---

## Manual verify on Expo Go

- [ ] **Home homeMode flash:** Open app as a `race` rider — confirm activity card does not briefly
  show "Last track prep" before switching to "Last session".
- [ ] **Tyre Wear Analysis — stale state:** Complete an analysis, navigate away, return — confirm
  prior photos/data are or are not cleared.
- [ ] **Gearing Guide — sprocket override:** Type an out-of-range value (e.g., front=5) in the
  override field — confirm it is rejected with user feedback before send.
- [ ] **Coach Chat timeout:** Kill API network mid-conversation — confirm UI shows an error within
  a reasonable window (not indefinite spinner).
- [ ] **Onboarding reset button:** Open Profile & settings in a production build (not `__DEV__`) —
  confirm reset is hidden or suitably gated.
- [ ] **Future-racer email:** Step through to racing-info email opt-in on a device with no email
  app configured — confirm user gets feedback rather than silent failure.
- [ ] **Track Memory Lakeside:** Open Track Memory Hub, select Lakeside Park, confirm layout loads
  and bike moves correctly (DEM-enriched terrain).

---

## Resolved since last review

_First weekly report — no prior report to compare against._

---

## Out of scope

- Pixel / visual QA, Maestro / detox E2E.
- iOS / Android simulator testing (no simulator available in cron context).
- App Store review status / Vercel promo page visual QA.
