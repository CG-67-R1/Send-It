# RoadRace App Review — 2026-07-13

## Executive summary

- **P0: 2** | **P1: 3** | **P2: 4**
- Repo gates pass locally (TSC, caches, calendar module). Production API is reachable but **`roadraceAi: false`** — Coach and Ask will fail until `OPENAI_API_KEY` is set on Render. Static MotoGP/ASBK calendar was still on 2025 dates (fixed in this pass). Render free-tier cold starts add 30–50s latency on first request after idle.

## Automated gates

```
Send-It mobile review preflight — 2026-07-13
API_URL=https://send-it-ke7r.onrender.com
SKIP_SCRAPERS=1

  OK  9 screens listed
  OK  TypeScript (app)
  OK  server.js / qa.js / roadraceAi.js syntax
  OK  retrieveForAsk: 5 chunks, fromKb=true
  OK  AU cache: 97 headlines (updated 2026-07-13)
  OK  AU calendar cache: 66 events (updated 2026-07-10)
  OK  calendar aggregation: 104 events
  OK  API /health, /headlines (152), /calendar (104)

Production /health: {"ok":true,"roadraceAi":false}
```

## Recent changes reviewed

- Initial Hermes autonomy roadmap implementation pass
- No prior RR_REVIEW_*.md to compare

## Per-screen findings

### HeadlinesScreen / HeadlinesListScreen
- [P2] Docs still describe 4-tab layout; app has 5 tabs

### CalendarScreen
- [P0] Static calendar had 2025 MotoGP/ASBK dates — **fixed** to 2026 in `api/data/calendar-static.json`

### QAScreen (Ask + Trivia)
- [P0] Ask tab depends on `OPENAI_API_KEY` on Render — currently missing
- [P1] `PROJECT_STATUS_AND_PRE_PRODUCTION.md` says Ask uses `/roadrace-ai/chat`; actual endpoint is `/roadrace-ai/ask`

### RiderCoachScreen
- [P0] Coach/Bike Setup broken in production until `OPENAI_API_KEY` set on Render
- [P1] Coach uses FAQs only, not full Q&A knowledge base

### TrackWalkScreen
- [P2] Voice notes need device permission testing on physical iOS

## Coding improvements

- [P1] No rate limiting on `/roadrace-ai/*` — OpenAI cost exposure
- [P1] `health-check.mjs` did not assert `roadraceAi: true` on live API — **fixed** in this pass
- [P2] Legacy `TRACKDAY_RIDER_AI_URL` constant unused in any screen
- [P2] No EAS/TestFlight pipeline for standalone iOS binary

## Cross-cutting

- **API:** App defaults to production Render in dev — good for Expo Go testers without local machine
- **Cold starts:** Render free tier sleeps; first request ~37s observed in this review
- **Autonomous ops:** Hermes cron local-only; no GitHub Actions CI until this pass
- **AU caches:** Refreshed locally; need scheduled cloud refresh (GitHub Action added)

## Suggested fix order (for Cursor)

1. **Set `OPENAI_API_KEY` on Render** → verify `/health` returns `roadraceAi: true` (manual dashboard step — cannot be done from repo)
2. **Deploy calendar-static 2026 update + refreshed AU caches** → push to `main`
3. **Enable GitHub Actions** health ping + weekly cache refresh (added in this pass)
4. **Configure Hermes daily gate** with Telegram delivery (script added)
5. Add AI rate limiting (P1 follow-up)

## Manual verify on Expo Go

- [ ] Install Expo Go on iPhone
- [ ] `cd app && npx expo start` — scan QR
- [ ] Headlines: World and Aus feeds load
- [ ] Events: calendar shows 2026 MotoGP/ASBK rounds
- [ ] Q&A Ask: send question — expect reply after OpenAI key is set
- [ ] Q&A Trivia: start quiz
- [ ] Coach: send message in Coach and Bike Setup tabs
- [ ] Track Walk: save corner note, optional voice

## Resolved since last review

- N/A (first review)

## Out of scope

- Pixel/visual QA, Maestro/E2E, TestFlight distribution
