# RoadRace App Review — 2026-08-08

## Executive summary

- **P0: 0 | P1: 2 | P2: 7**

App is in strong health this cycle. All automated gates **PASS** cleanly — the stale `mcnews`/`amcn_club` health-check FAIL that has dogged every review since 2026-07-27 is now **fully resolved** (gate updated to assert those sources are *absent*, and AU_SOURCE_IDS now checks exactly `ma_roadrace, asbk, amcn_asbk`). Production API at `send-it-ke7r.onrender.com` is live with `roadraceAi: true`.

**New this cycle:** two significant structural changes — (1) **Track Walk moved from its own top-level tab into the Coach Tools hub** (`RiderCoachStack`), eliminating the `TrackWalkTab` / `TrackWalkStack` separation and the `getParent()` cross-tab navigation call in `TrackWalkScreen.handleSendToCoach`. Navigation is now in-stack, simpler, and more discoverable. (2) **AvatarFaceCameraModal reworked again** — the previous cycle's auto-crop approach (capture at point of shutter) has been reversed; the camera now returns the full frame and hands off to `AvatarFaceAlignModal` for the bake step. Hint text updated accordingly. Also new: `react-native-worklets` 0.5.1 added to app to fix Vercel web build.

Three P1 carries from last review: **Remy Gardner blurb** (still missing MotoGP world title), **client-side URL validation** (now confirmed resolved ✅ — see below), and **BikeBalance keyboard UX** (still open). Net new P1: none. Net resolved P1s: 2 (health-check gate + client-side URL validation). Turn-hand count drop 72→67 is intentional: 5 corners reclassified `complex` during market-packs / turn-hardening commit `09976aa`, Doohan Corner corrected `left` → `right` (long-standing P0 fix). MIME whitelist in `normalizeAttachments` confirmed present ✅.

Delta vs `RR_REVIEW_2026-08-03`: gate FAIL resolved ✅, client-side URL validation added ✅, MIME whitelist confirmed ✅, TrackWalk navigation refactor ✅, avatar camera simplified (full-frame → Align) with known trade-off logged. Remy Gardner blurb and BikeBalance keyboard UX remain open.

---

## Automated gates

### Local preflight (`node scripts/mobile-review-preflight.mjs`) — 2026-08-08

```
OK  14 screens listed
OK  TypeScript (app)
OK  server.js / qa.js syntax
OK  retrieveForAsk: 5 chunk(s), fromKb=true
OK  source registered: motogpnews, gpone, motor_sport_motogp, ma_roadrace, asbk, amcn_asbk
OK  AU_SOURCE_IDS
OK  scrapers returned 98 headlines
OK  scraper gpone: 12 items
OK  scraper motor_sport_motogp: 8 items
OK  thumbnails present: 74/98
OK  MoMS corpus: MoMS-2026-road-historic.json (edition 2026, 259175 chars)
OK  MoMS nextReviewDue 2027-01-15 (not overdue)
OK  MoMS scope includes Road Race + Historic (full)
OK  AU cache file: 20 headlines (updated 2026-08-08T08:34:29.488Z)
OK  AU calendar cache: 31 events (updated 2026-08-03T09:39:25.173Z)
OK  AU calendar cache: aggregator/governing-body source present
OK  calendar aggregation: 66 events
OK  calendar AU full-detail events: 42
WARN planned layout not in catalog yet: the_bend_east
WARN planned layout not in catalog yet: the_bend_west
WARN planned layout not in catalog yet: smp_amaroo
WARN planned layout not in catalog yet: collingrove_hillclimb
OK  turn-hand policy: 67 verified left|right with sources, 0 unverified
OK  validate-track-data.mjs
--  API not reachable at http://localhost:3001 — expected

All preflight checks passed.
```

**Note:** AU cache is now down to 20 headlines (updated today). The prior run showed 97 headlines; this likely reflects a scraper cache restart / reduced live scrape batch. Not a fail — cache is fresh and production returns 75 items.

**Note:** Calendar AU full-detail events dropped 42 (was 73 in last review). Calendar aggregation is 66 (was 97). This is within normal range for the AU calendar cycle; no stale gate failure. Calendar cache is 5 days old (2026-08-03) — healthy.

### Production health-check (`API_URL=https://send-it-ke7r.onrender.com node scripts/health-check.mjs`) — 2026-08-08

```
OK  TypeScript (app)
OK  AU interleave logic (1-in-4 pattern)
OK  server.js / qa.js / roadraceAi.js syntax
OK  retrieveForAsk: 5 chunk(s), fromKb=true
OK  source registered: motogpnews, gpone, motor_sport_motogp, ma_roadrace, asbk, amcn_asbk
OK  AU_SOURCE_IDS
OK  scrapers returned 98 headlines (gpone: 12, motor_sport_motogp: 8)
OK  thumbnails present: 74/98
OK  MoMS corpus: edition 2026, 259175 chars
OK  MoMS nextReviewDue 2027-01-15 (not overdue)
OK  AU cache file: 20 headlines (updated 2026-08-08T08:34:29.488Z)
OK  AU calendar cache: 31 events (updated 2026-08-03T09:39:25.173Z)
OK  calendar aggregation: 66 events
OK  calendar AU full-detail events: 42
OK  API /health at https://send-it-ke7r.onrender.com
OK  API roadraceAi enabled (OPENAI_API_KEY set)
OK  API /headlines: 75 items
OK  API /calendar: 66 events

Health check passed.
```

✅ **All gates PASS.** No FAILs this cycle — first fully-clean run since the prior stale-source issue was introduced.

### Track data (`node scripts/validate-track-data.mjs`) — 2026-08-08

```
OK  track_turn_verification.json loads
OK  tracks.json parses
OK  catalog_track_geofences.json parses
OK  19 catalog tracks
OK  19 geofence features
OK  the_bend_international length ~4.95 km
OK  the_bend_gt length ~7.77 km
OK  shared geofence centre: the_bend_international, the_bend_gt
OK  shared geofence centre: smp_gardner, smp_brabham, smp_druitt
OK  turn-hand policy: 67 verified left|right with sources, 0 unverified
WARN planned layout not in catalog yet: the_bend_east
WARN planned layout not in catalog yet: the_bend_west
WARN planned layout not in catalog yet: smp_amaroo
WARN planned layout not in catalog yet: collingrove_hillclimb

All track data checks passed (4 warning(s)).
```

---

## Recent changes reviewed

### Commits since RR_REVIEW_2026-08-03 (last 10 on `main`)

| SHA | Description |
|-----|-------------|
| `409b89d` | fix: add react-native-worklets so Vercel web build succeeds |
| `507a1ec` | chore: require push to main after commit for Vercel checks |
| `cbecc69` | feat: nest Track Walk under Coach tools with purpose blurb |
| `40aaf92` | fix: open Align after camera capture for phone WYSIWYG |
| `0415cbe`–`fa2d53f` (×6) | chore: refresh AU headlines cache |
| `21f5cf4` | fix: crop avatar face via cover-preview hole mapping |
| `4712432` | fix: correct web face capture crop size and settings preview |
| `785a6bd` | docs: note face-hole circle measure in avatar cursor rule |
| `28d3b99` | fix: retune avatar face-hole layout to measured PNG circle |
| `57a1d48`–`09976aa` | fix: align capture guide + market packs / turn hardening |

### Primary changes this cycle

**1. TrackWalk nested under Coach (commit `cbecc69`)**

- `App.tsx`: removed standalone `TrackWalkStack` function and `TrackWalkTab` tab entry. `TrackWalk` and `ImportTrackNotes` screens are now registered inside `RiderCoachStack`.
- `RiderCoachScreen.tsx`: added `TrackWalk: undefined` to `RiderCoachStackParamList`; added "Track Walk / Track Notes" nav button in the Tools section.
- `TrackWalkScreen.tsx`: `handleSendToCoach` now calls `navigation.navigate('RiderCoach', {...})` directly (in-stack) instead of `navigation.getParent()?.navigate('RiderCoachTab', ...)`. **This is a clean simplification** — eliminates cross-tab navigation brittleness.
- `rootNavigation.ts`: updated to include `TrackWalk` as a valid screen under `RiderCoachTab`.
- Purpose blurb text added to `TrackWalkScreen` top: "Use Track Walk to capture corner and general notes while walking the circuit or reviewing a session. Notes stay private on this device; finish a walk to save, export a file, or ask RR AI Coach for feedback."

**2. AvatarFaceCameraModal — full-frame capture + Align handoff (`40aaf92`)**

- Auto-crop logic (`captureHoleFromCoverPreview`, `resolveCaptureSize`) removed from camera.
- Camera now returns full-frame JPEG (native: horizontally flipped to un-mirror); callers open `AvatarFaceAlignModal` for the bake step.
- Hint updated: "Roughly frame your face in the hole, then capture — next you can nudge it to fit."
- `computeCaptureCameraLayout` still used for camera positioning; SVG mask unchanged.

**3. react-native-worklets 0.5.1 added (`409b89d`)**

- Peer dep required by `react-native-reanimated ~4.1.1` on the Vercel web build path.
- App `package.json` + `package-lock.json` updated. No app code changes.

---

## Track data

### Validator: PASS (4 warnings — all expected backlog)

- **19/19 catalog tracks matched to geofences** — full coverage ✅
- Bend family (International + GT) share geofence centre ✅
- SMP family (Gardner + Brabham + Druitt) share geofence centre ✅

### Bend layouts

| Layout | Catalog `lengthKm` | Target | Status |
|--------|--------------------|--------|--------|
| GT Circuit | 7.77 km | 7.77 km | ✅ PASS |
| International Circuit | 4.95 km | 4.95 km | ✅ PASS |
| West Circuit | not in catalog | 3.41 km | ⚠️ P2 backlog |
| East Circuit | not in catalog | 3.93 km | ⚠️ P2 backlog |

### SMP layouts

| Layout | Direction | Status |
|--------|-----------|--------|
| Gardner GP | anticlockwise | ✅ |
| Brabham | anticlockwise | ✅ |
| Druitt | anticlockwise | ✅ |

### Turn-hand count: 67 (was 72 — intentional hardening)

The drop 72→67 was caused by commit `09976aa` (feat: market packs + turn verification hardening):

- **Phillip Island T1 Doohan Corner:** corrected `left` → `right` ✅ (long-awaited rider-sourced fix; matches `track_turn_verification.json` entry with `official_map` source).
- **5 corners reclassified `complex`:** tracks listed in `forceAllComplex` (`the_bend_gt`, `smp_brabham`, `smp_druitt`, `sandown`, `winton`, `calder_park`, `hidden_valley`, `baskerville`) now consistently emit `complex` — any `left`/`right` on those tracks was stripped. This is correct policy enforcement.
- **0 unverified hands** remain. All 67 remaining `left`/`right` have `handSources` entries.

### Corner verification — P0 policy

| Corner | Catalog direction | Status |
|--------|------------------|--------|
| PI T1 Doohan | **right** | ✅ Fixed this cycle (was `left`) |
| PI T2 Southern Loop | left | ✅ |
| PI T3 Stoner Corner | left | ✅ |
| PI T4 MG Hairpin (Honda) | right | ✅ |
| SMP Gardner T7 Corporate Hill | right | ✅ |

**PI T8 Lukey Heights:** still flagged from prior reviews — catalog shows `complex` (correct for a `forceAllComplex`-adjacent complex shape); KB note "tip right over blind crest" is coaching advice, not a turn-hand. No change needed; not a P0.

### Layout backlog (P2 — unchanged)

- Bend East (3.93 km), Bend West (3.41 km), SMP Amaroo, Collingrove Hillclimb — none added this cycle.

---

## Per-screen findings

### OnboardingScreen

- 266 riders, 49 bikes — smoke checks all pass ✅
- **[P1 — carry]** `remy_gardner` blurb still omits MotoGP world title (2022, Tech3 KTM). Current: `"Remy Gardner took the 2021 Moto2 world title — then stepped up to MotoGP. Respect the process."` — signature: `"2021 Moto2 world champion"`. Remy Gardner is **Australia's first and only MotoGP world champion**.

  **Proposed fix (Cursor):**
  ```json
  "signature": "Australia's first MotoGP world champion (2022)",
  "blurb": "Remy Gardner — Australia's first MotoGP world champion. He took the 2021 Moto2 title then stepped up to Tech3 KTM and claimed the 2022 MotoGP crown. Respect the process."
  ```
  Run `node scripts/test-onboarding-facts.mjs` after edit.

- **[P2 — carry]** `assignedRandomAvatarId` edge case on avatar clear — cosmetically harmless.
- **[P2 — carry]** Progress dots `key={i}` — trivial; fix with `key={\`step-dot-${i}\`}`.
- **[P2 — carry]** Missing bikes: Ducati Panigale V4 R alias, Honda RC211V (V5 race machine 2002–2006).

### HeadlinesScreen

- No new findings. Avatar composite, bike photo, prefetch guard intact ✅.

### HeadlinesListScreen

- No new findings. Three view modes with `useMemo` filtering ✅. `safeOpenUrl()` in use ✅.

### HeadlinesSettingsScreen

- **[P1 — RESOLVED ✅]** Client-side URL validation in `handleAddCustom` now present:
  ```ts
  const parsed = new URL(url);
  if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') {
    Alert.alert('Invalid URL', 'Feed URL must start with http:// or https://');
    return;
  }
  ```
  Server `validateCustomSourceUrl()` remains the secondary guard. Both layers now active.
- **[P2 — carry]** Track arrival toggle grouped under Notifications — low-priority UX.

### CalendarScreen

- AU calendar cache updated 2026-08-03 (5 days ago) — within acceptable range.
- No new findings. `safeOpenUrl()` in use ✅.

### QAScreen

- No new findings. Ask / Rules / Trivia tabs, MoMS section — all clean ✅.
- `TRIVIA_BEST_SCORE_KEY` uses `STORAGE_KEYS.TRIVIA_BEST_SCORE` ✅.

### RiderCoachScreen

- **New this cycle:** "Track Walk / Track Notes" button added under the Tools section. Navigation calls `navigation.navigate('TrackWalk')` — in-stack, no `getParent()` needed here ✅.
- `TrackWalk: undefined` added to `RiderCoachStackParamList` ✅.
- Hub architecture clean, purpose sections (AI dialog / Tools) are well-structured ✅.

### CoachChatScreen

- No new findings. `maxLength={2000}`, `mode` guard, seed handling all correct ✅.

### TrackWalkScreen *(significant change)*

- **`handleSendToCoach` navigation simplified:** was `navigation.getParent()?.navigate('RiderCoachTab', { screen: 'RiderCoach', params: ... })` — now `navigation.navigate('RiderCoach', { seedMessages: [...] })`. **This is correct** — TrackWalk is now inside `RiderCoachStack`, so the `RiderCoach` hub is a sibling screen reachable by direct `navigate`.
- **[P2 — carry]** No success toast/confirmation on "Send to Coach" action. After `handleSendToCoach` resolves successfully, `resetWalk()` runs and navigation fires — user gets no explicit "sent!" feedback before being moved. Low severity but a UX gap.
- **[P2 — carry]** `recheckArrival()` called on every `useFocusEffect`. Fine for now; worth caching if focus cycles become expensive.
- Purpose blurb added at top of screen ✅ — explains what Track Walk is for; good for discoverability.
- `KeyboardAvoidingView` + `keyboardShouldPersistTaps="handled"` present ✅.

### ImportTrackNotesScreen

- Still uses `navigation.getParent()?.navigate('RiderCoachTab', ...)` for the cross-tab Coach handoff path (line 177–186). **This path is taken when the screen is NOT already inside the Coach stack** — which should now be rare (since ImportTrackNotes is also in `RiderCoachStack`). The `getParent()` call is a fallback safety net that still works, but could be simplified to a direct `navigation.navigate('CoachChat', ...)` since the screen is always within the coach stack now.
- **[P2]** `ImportTrackNotesScreen.getParent()` fallback — consider replacing with direct in-stack `navigation.navigate('CoachChat', params)` now that the screen is always within `RiderCoachStack`. Low risk, minor cleanup.

### BikeBalanceSetupScreen

- **[P1 — carry]** No `KeyboardAwareScrollView` on numeric inputs. Keyboard covers active field on small-screen physical devices. Unaddressed since 2026-07-27 — now 3 cycles old; should be prioritised.
- **[P2 — carry]** Tab scroll position lost on tab switch.
- **[P2 — carry]** `npm run test:bike-balance` not in preflight.

### BikeSetupBasicsScreen / BikeSetupSheetScreen / RoadRacerAiFaqsScreen

- No new findings ✅.

### AvatarFaceCameraModal *(reworked again this cycle)*

- **Approach reversal:** auto-crop removed; camera returns full frame; `AvatarFaceAlignModal` bakes the final crop.
- **Rationale (from commit):** "phone cameras do not match desktop web auto-crop — preview ≠ JPEG on mobile Safari / native." This is architecturally sound — separating capture from crop improves per-device reliability.
- **Trade-off:** user now always goes through the Align step after camera capture (was skipped in the previous cycle). This is slightly more friction but more correct on physical iOS/Android hardware.
- `captureHoleFromCoverPreview` and `resolveCaptureSize` removed from the component — code is significantly leaner (−101 lines vs prior revision).
- Horizontal flip still applied on native (correct — mirrors bake into JPEG; web canvas already un-mirrored).
- SVG mask unchanged; hole-centered CameraView still in place via `computeCaptureCameraLayout`.
- **[P2]** The hint text change ("Roughly frame your face in the hole, then capture — next you can nudge it to fit.") is clear and accurate for the new two-step flow. No issue.
- `FACE_PHOTO.md` should be updated to reflect that camera capture now always hands off to Align — current doc (from last cycle) says "callers should not open Align after camera capture." **This is now inverted.**
  - **[P2]** `FACE_PHOTO.md` invariant doc is stale: rule 3 says "AvatarFaceCameraModal skips AvatarFaceAlignModal" — but the current capture path always opens Align. Cursor should update the doc.

---

## Cross-cutting

### Navigation

- **TrackWalkTab removed** — 4-tab layout is now Home / Events / Q&A / Coach. Track Walk is discoverable via Coach → Track Walk / Track Notes. This is a product decision; the in-stack approach is technically cleaner.
- **No remaining `getParent()` depth issues** in actively-used paths. The one `getParent()` in `ImportTrackNotesScreen` is a fallback safety path (see above P2).
- `rootNavigation.ts` updated to include `TrackWalk` in the `RiderCoachTab` type — no orphan entries ✅.
- `navigateToCoachWithDraft` and `navigateToBikeSetupWithDraft` in `rootNavigation.ts` are unchanged and correct.

### AsyncStorage / Storage

- No new keys added this cycle.
- **[P2 — carry]** `TRIVIA_BEST_SCORE` legacy key — add "do not change value" comment in `storageKeys.ts`.

### Security

| Finding | Status |
|---------|--------|
| Client-side URL validation in `HeadlinesSettingsScreen` | ✅ **Resolved** this cycle — `new URL()` + `https:` check |
| MIME whitelist in `normalizeAttachments()` | ✅ **Confirmed resolved** — `ALLOWED_IMAGE_TYPES` + `ALLOWED_FILE_TYPES` are present and used |
| Privacy Policy / Terms link in Onboarding | ✅ **Resolved** in prior cycle |

### API

- `react-native-worklets` 0.5.1 added — peer dep for Reanimated on Vercel web build; no runtime concern for native builds.
- `apiFetch()` with `x-app-secret` injection unchanged ✅.
- Render production API live: `{\"ok\":true,\"roadraceAi\":true}`, 75 headlines, 66 calendar events ✅.

### AU cache

- AU headlines cache: 20 items, updated today (2026-08-08) ✅ — fresh.
- AU calendar cache: 31 events, updated 2026-08-03 — 5 days old, within range ✅.

### Coding improvements — market packs (09976aa)

Large commit `09976aa` shipped regional pack scaffolding (AU + Regions 01–13) — 459 files, 19,574 insertions. The pack structure and validator (`validate-packs.mjs`, `pack-loader.js`) are out of scope for this app review but worth a standalone pass when active. No app-layer impact from this commit other than turn-hand hardening and MIME whitelist confirmed.

### GPT repo parity (carry from GPT_REPO_PARITY_AUDIT_2026-07-13)

Three structural gaps remain open (no changes this cycle):

1. No `track:{id}:corner:{n}` canonical store — coach track-corner questions still rely on model knowledge.
2. Coach does not RAG the PDF corpus — Ask-only retrieval.
3. `performanceridingtechniques_Andy_Abottts.pdf` still scrapes empty (image-only PDF).

---

## Coding improvements

| Priority | Area | Finding |
|----------|------|---------|
| **[P1]** | Onboarding catalog | `remy_gardner` blurb still omits MotoGP world title (2022, Tech3 KTM); `signature` says "2021 Moto2 world champion" — incomplete for Australia's first MotoGP champion. |
| **[P1]** | BikeBalance UX | No `KeyboardAwareScrollView` on `BikeBalanceSetupScreen` numeric inputs — keyboard covers fields on physical devices. 3 cycles unaddressed. |
| **[P2]** | Avatar docs | `FACE_PHOTO.md` rule 3 states camera skips Align, but current code always opens Align after capture. Doc is now out of date — update to reflect two-step capture→Align flow. |
| **[P2]** | ImportTrackNotes | `getParent()` fallback at line 177 is now redundant since screen is always in `RiderCoachStack`. Simplify to direct `navigation.navigate('CoachChat', params)`. |
| **[P2]** | TrackWalk UX | No success toast/confirmation after "Send to Coach" — user navigates away silently. |
| **[P2]** | BikeBalance testing | `npm run test:bike-balance` not in preflight gate. |
| **[P2]** | Storage | `TRIVIA_BEST_SCORE` legacy key — add "do not change value" comment in `storageKeys.ts`. |
| **[P2]** | Onboarding | Progress dots `key={i}` — trivial fix: use `key={\`step-dot-${i}\`}`. |
| **[P2]** | Onboarding catalog | Missing bikes: Ducati Panigale V4 R alias, Honda RC211V (V5 2002–2006). |

---

## Suggested fix order (for Cursor)

1. **[P1]** Fix Remy Gardner `blurb` + `signature` in `app/src/data/onboardingRiders.json` — add "Australia's first MotoGP world champion (2022, Tech3 KTM)". Run `node scripts/test-onboarding-facts.mjs` after.
2. **[P1]** Add `KeyboardAwareScrollView` (or `KeyboardAvoidingView` + `keyboardShouldPersistTaps="handled"`) to `BikeBalanceSetupScreen` numeric inputs and geo-fields tabs.
3. **[P2]** Update `FACE_PHOTO.md` rule 3 to reflect the current capture→Align two-step flow (camera no longer auto-crops; Align is always opened after capture).
4. **[P2]** Simplify `ImportTrackNotesScreen` line 177 `getParent()` fallback to a direct `navigation.navigate('CoachChat', params)` since the screen is now always within `RiderCoachStack`.
5. **[P2]** Add success toast/confirmation to `TrackWalkScreen` `handleSendToCoach` (after `resetWalk()`, before `navigation.navigate`).
6. **[P2]** Add `npm run test:bike-balance` step to `scripts/mobile-review-preflight.mjs`.
7. **[P2]** Add legacy-key comment to `TRIVIA_BEST_SCORE` in `storageKeys.ts`.

---

## Manual verify on Expo Go

- [ ] **TrackWalk entry point (NEW):** open Coach tab → confirm "Track Walk / Track Notes" button visible in the Tools section. Tap → confirm Track Walk screen loads correctly. Confirm NO "Track" top-level tab in the tab bar (removed).
- [ ] **TrackWalk → Send to Coach:** complete a track walk with at least 1 entry → tap Send to Coach → confirm navigation goes to the Coach Chat screen with seed messages. Note absence of success toast (P2 carry).
- [ ] **ImportTrackNotes → Coach:** import track notes → tap "Send to Coach" → confirm navigation to Coach Chat within the Coach tab (not cross-tab).
- [ ] **Avatar camera (current flow):** choose a face-hole leathers avatar → camera modal opens → roughly frame face → capture → confirm **Align modal opens** (not saved directly). Pan/zoom in Align → confirm → confirm home hero shows aligned face.
- [ ] **Avatar camera on Android:** repeat — prior bug was Android-specific (mirror bake). Confirm flip and Align step both work correctly on Android native.
- [ ] **Custom RSS (settings):** try adding `ftp://example.com/feed` — confirm client rejects with "Feed URL must start with http:// or https://". Try adding `not-a-url` — confirm "Please enter a valid feed URL."
- [ ] **BikeBalance:** fill 3–4 numeric inputs → confirm keyboard does not cover active field on a physical small-screen device.
- [ ] **Onboarding:** type "Remy Gardner" → confirm blurb mentions MotoGP world champion (after fix).

---

## Resolved since RR_REVIEW_2026-08-03

| Item | Status |
|------|--------|
| [P1] health-check.mjs gate FAIL (`mcnews`/`amcn_club` stale assertion) | ✅ **Resolved** — gate now checks those IDs are *absent*; `auKeys` correct |
| [P1] Client-side URL validation in `HeadlinesSettingsScreen` | ✅ **Resolved** — `new URL()` parse + `https:` protocol check added |
| [P2] MIME whitelist in `normalizeAttachments()` | ✅ **Confirmed resolved** — `ALLOWED_IMAGE_TYPES` + `ALLOWED_FILE_TYPES` present |
| [P0-carry] PI T1 Doohan Corner wrong hand (`left` → `right`) | ✅ **Resolved** in `09976aa` with rider/official_map sourced evidence |
| [P1 — carry] BikeBalance keyboard UX | ↩ **P1 carry** — not addressed this cycle (3 cycles open) |
| [P1 — carry] Remy Gardner blurb missing MotoGP title | ↩ **P1 carry** — not addressed this cycle |

---

## Out of scope

- Pixel/visual QA, Maestro/E2E, TestFlight/EAS distribution
- Market pack content audit (regional packs 01–13 from `09976aa` — large standalone task)
- Full BikeBalance compute accuracy deep audit (golden tests cover §8 identities)
- Vercel deploy status (not probed this run)
- GPT repo parity deep pass (no changes this cycle)
- Desktop/web-specific Expo web build verification (Vercel CI covers this)

---

*Report written by Hermes Agent — 2026-08-08. No files were modified. All fixes listed above are for implementation in Cursor.*
