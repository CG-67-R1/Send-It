# RoadRace App Review — 2026-08-10

## Executive summary

- **P0:** 0 | **P1:** 3 | **P2:** 7
- First weekly review; no prior `RR_REVIEW_*.md` to delta against. All automated gates pass cleanly — TypeScript, scrapers, AU interleave, MoMS rule book, and the production Render API are healthy. The app is in a stable, shippable state after a productive recent sprint (37 files, 660 insertions across avatar align/camera, BikeBalance variable renames, track arrival, and TrackWalk nesting under Coach tools). Three P1s surfaced: the `TRIVIA_BEST_SCORE` storage key sits outside the `STORAGE_KEYS` namespace convention; the Screen Brief is behind on new screens (BikeBalance, BikeSetupBasics, CoachChat); and `as never`/`as any` casts in navigation create a runtime blind spot. Track data is fully PASS with 4 planned-layout warnings (Bend East/West, SMP Amaroo, Collingrove — expected backlog).

---

## Automated gates

### mobile-review-preflight.mjs — PASS

```
OK  14 screens listed
OK  TypeScript (app)
OK  server.js / qa.js syntax
OK  Track data — all checks passed (4 warnings — planned layouts not in catalog yet)
OK  AU interleave logic (1-in-4 pattern)
OK  API scrapers: 98 headlines, gpone: 12, motor_sport_motogp: 8
OK  MoMS corpus 2026 edition, nextReviewDue 2027-01-15
OK  AU cache: 35 headlines (updated 2026-08-08)
OK  AU calendar cache: 31 events (updated 2026-08-03)
OK  Calendar aggregation: 66 events, 42 AU full-detail events
--  Local API not running (expected for cron context)
```

### health-check.mjs — PASS (production API_URL=https://send-it-ke7r.onrender.com)

```
OK  API /health
OK  roadraceAi enabled (OPENAI_API_KEY set)
OK  API /headlines: 86 items
OK  API /calendar: 66 events
```

### validate-track-data.mjs — PASS (4 warnings)

```
OK  track_turn_verification.json loads
OK  tracks.json parses
OK  catalog_track_geofences.json parses
OK  19 catalog tracks, 19 geofence features
OK  the_bend_international length ~4.95 km
OK  the_bend_gt length ~7.77 km
OK  shared geofence centre: the_bend_international, the_bend_gt
OK  shared geofence centre: smp_gardner, smp_brabham, smp_druitt
OK  turn-hand policy: 67 verified left|right, 0 unverified
WARN planned layout not in catalog yet: the_bend_east
WARN planned layout not in catalog yet: the_bend_west
WARN planned layout not in catalog yet: smp_amaroo
WARN planned layout not in catalog yet: collingrove_hillclimb
```

---

## Recent changes reviewed (last 10 commits)

| Commit | Summary |
|--------|---------|
| `794d61d` | Merge branch main |
| `7923159` | fix: allow Vercel web origins in API CORS |
| `8e484eb` | chore: refresh AU headlines cache |
| `794d0f1` | chore: refresh AU headlines cache |
| `a9b91d2` | feat: add Flip control on Align face for mirror correction |
| `1e26600` | chore: refresh AU headlines cache |
| `93f97ba` | fix: disable mirrored avatar face capture |
| `5395ed7` | refactor: rename unclear local variables for readability |
| `1bea958` | fix: track arrival default on, KB avoid on BikeBalance, AU source list cleanup |
| `409b89d` | fix: add react-native-worklets so Vercel web build succeeds |

**Key files touched:** `AvatarFaceAlignModal.tsx` (Flip control, 118 lines changed), `AvatarFaceCameraModal.tsx` (mirror-off fix), `TrackWalkScreen.tsx` (nesting under Coach tools), `BikeBalanceSetupScreen.tsx` + full `bikeBalance/` calc stack (variable rename refactor), `server.js` (CORS), `scrapers.js` (AU source cleanup), `faceHoleGeometry.ts` (calc overhaul).

**No regressions found in reviewed changes.** The mirror-fix and Flip control are correctly applied in both camera and library paths.

---

## Track data

### Structural validation

- **Result: PASS** — validator exits 0.
- 19 catalog tracks, 19 geofence features — 1-to-1 match.
- Bend International: **4.95 km ✓** | Bend GT: **7.77 km ✓** (matches ground truth).
- Turn-hand policy clean: 67 verified hands with sources, 0 unverified.

### Catalog vs geofence coverage

All 19 catalog `trackId`s have a matching geofence. No orphan geofences.

### Corner QA

- **Phillip Island (anticlockwise):** Doohan T1 = **right** ✓ (locked, correct — not inferred from CCW). Southern Loop T2 = **left** ✓. Stoner T3 = **left** ✓. Miller T4 = **right** ✓. Policy intact.
- **SMP Gardner (anticlockwise):** direction correct ✓. T1 left, T2 left (Southern Hairpin), T3 right (over tunnel), T4 right (Corporate Hill), T5 left — structurally sound.
- **The Bend GT:** 10 corners, clockwise direction. No `direction: straight` on Hairpin/Sweeper/Double-apex shapes found.
- **No double-space labels detected.**
- **No unbalanced parentheses found in initial pass.**

### Approximate lengths flagged

Three tracks use `~` prefix strings (not numeric): `smp_brabham: ~3.0 km`, `smp_druitt: ~1.9 km`, `calder_park: ~2.3 km`. These are correct qualitative values but will fail any strict numeric length comparison. Not a P0 since validator passes, but worth noting.

### Layout backlog (P2 — expected)

| Layout | Status |
|--------|--------|
| `the_bend_east` (3.93 km) | Not in catalog — planned |
| `the_bend_west` (3.41 km) | Not in catalog — planned |
| `smp_amaroo` | Not in catalog — planned |
| `collingrove_hillclimb` | Not in catalog — intentional extra |

---

## Per-screen findings

### OnboardingScreen

- [P2] `totalSteps = 8` hard-coded in-file. If steps change, this constant silently diverges from actual step count (currently 8 steps: 0–7). Consider deriving from step array or at least asserting in dev.
- Flow: welcome → bike → rider → taste bridge → activity → future racer (conditional) → nickname+avatar → summary. Camera → Align modal → face bake pipeline is correct per FACE_PHOTO.md. Random mascot assignment on summary step works. No P0/P1.

### HeadlinesScreen (Home)

- [P1] `tabNav?.navigate('CalendarTab' as never)` and `tabNav?.navigate('RiderCoachTab' as never)` use `as never` to bypass TS tab navigation types. If a tab is renamed, this fails silently at runtime with no compile error. Should use typed `RootTabParamList` navigation or a named `rootNavigation` helper.
- [P2] `tabNav?.navigate('Q&A' as never)` — tab named `'Q&A'` with ampersand is unusual; brittle if tab name changes.
- Prefetch on mount with `AbortSignal.timeout(25000)` is correct. Photo load/clear, avatar composite, face URI all correctly `useFocusEffect`-refreshed.

### HeadlinesListScreen

- No P0/P1. Custom source/priority merge handles retired sources correctly. `?refresh=1` bypass is implemented. Thumbnail fallback on error is present.
- [P2] `viewMode` default is `'local'` — matches product intent but brief says "Latest headlines" implying world-first; may want to confirm with product.

### HeadlinesSettingsScreen

- No P0/P1. Avatar align + camera pipeline duplicated here (same as OnboardingScreen) — correct but raises maintainability concern (see Coding Improvements).
- Track arrival toggle, permission denied → Settings link, camera permission denied → Settings link all present.

### CalendarScreen

- `AbortSignal.timeout(15000)` ✓. Filter modes (all/local/world) present. `isUpcomingOrOngoing` guard correct. `expo-calendar` import present for add-reminder flow.
- [P2] `SERIES_COLORS` has duplicate key semantics (`'asbk'` and `'ASBK'` both defined — JS takes last). Harmless since both map to same amber, but sloppy.

### QAScreen

- **Rules tab present** (separate from Ask) — not mentioned in SCREEN_BRIEF_FOR_VISUALS.md §5 (Brief is stale on this). MoMS online meta surfaced to UI. [P2 — brief lag]
- **Trivia does NOT use GPT** — confirmed. Uses `QA_TRIVIA_URL` only.
- [P1] `TRIVIA_BEST_SCORE` key in `STORAGE_KEYS` is `'ROADRACER_TRIVIA_BEST'` — non-prefixed (other keys all use `@roadrace_*` pattern). Will not collide, but breaks the centralised-key contract and makes future audits harder.
- Goat explosion animation: `setTimeout` used inside component. Timer cleared in `useEffect` return — correct.
- `triviaFeedbackTimerRef` cleanup on unmount ✓.

### RiderCoachScreen

- Hub screen: AI dialog section (Coach / Bike Setup), Tools section (Day Setup Sheet, Bike Balance Setup, Bike Setup Basics, Track Walk / Track Notes, RoadRacer AI FAQs). Clean, scrollable.
- Deep-link `seedMessages` / `seedDraftMessage` / `seedTab` → `navigation.replace('CoachChat', …)` pattern is correct.
- [P2] Screen Brief (§6 Rider Coach) describes old "Coach | Bike Setup tab bar" layout — now replaced by hub+navigate pattern. Brief is significantly stale here. Does not affect runtime but could mislead visual/design work.

### CoachChatScreen

- `mode` param strictly typed, guarded: `route.params?.mode === 'bikesetup' ? 'bikesetup' : 'coach'`. ✓
- Conversation generation ref prevents stale async responses. Seed dedup via JSON stringify ref. ✓
- `AbortSignal.timeout(90000)` on coach chat — long but reasonable for LLM. ✓
- No P0/P1.

### TrackWalkScreen

- Track walk nested under Coach Tools (via `cbecc69`) — correct per AGENTS.md.
- `catch {}` on two speech-recognition paths (lines 231, 262) — these are optional-native-module errors so silent catch is acceptable; however, no user-visible error means user gets no feedback if speech fails for a non-module reason. [P2]
- `sessionReadyForCoach` guard before send ✓. `deleteTrackWalkSession` with confirmation ✓.
- `isTrackArrivalEnabled` → `useTrackArrivalRecheck` context correctly used for overlay. ✓

### ImportTrackNotesScreen

- [P1] `const tabNav = navigation.getParent() as ...` with explicit cast to untyped shape — similar pattern to HeadlinesScreen. Should use `rootNavigation.navigateToCoachWithDraft()` helper that already exists in `app/src/navigation/rootNavigation.ts`. The helper is not used here, creating a dual-path risk.
- Clipboard paste, photo attach, track picker, `OtherTrackContextForm` all present. Error handling on send (setSending(false) in finally) ✓.

### BikeSetupBasicsScreen / BikeSetupSheetScreen / BikeBalanceSetupScreen

- Not in SCREEN_BRIEF_FOR_VISUALS.md at all — these screens are invisible to the brief. [P2 — brief lag]
- [P1] `BikeSetupBasicsScreen.tsx:106` uses `} as any)` — needs review. May be an ImagePicker result type narrowing issue.
- `bikeBalance/` calc stack: recent refactor renamed variables for readability — no type regressions found (TSC passes).

### RoadRacerAiFaqsScreen

- Not in SCREEN_BRIEF_FOR_VISUALS.md. [P2 — brief lag]
- No code issues identified in import scan.

### AvatarFaceCameraModal / AvatarFaceAlignModal

- Mirror fix (`a9b91d2`) and Flip control (`a9b91d2`) correctly implemented.
- `useId()` for mask DOM id ✓. `PanResponder` + scale clamped [0.6, 3.5] ✓.
- `computeFaceHole` / `computeCaptureCameraLayout` / `computeCaptureGuide` correctly separated per `FACE_PHOTO.md` invariants.

---

## Cross-cutting

### Navigation

- `getParent()` used in `HeadlinesScreen` and `ImportTrackNotesScreen`. Depth is 1 (stack inside tab) — acceptable, but `as never` typing is brittle.
- `rootNavigation.navigateToCoachWithDraft()` exists and is typed but not used by ImportTrackNotesScreen — divergence risk.
- **No `getParent().getParent()` (depth 2+) found** — good.

### AsyncStorage / Storage

- Central `STORAGE_KEYS` registry in `app/src/constants/storageKeys.ts` covers 21 keys.
- Key inconsistency: `TRIVIA_BEST_SCORE` = `'ROADRACER_TRIVIA_BEST'` (no `@roadrace_` prefix) vs all others.
- `trackGeofence.ts` uses two local constants (`KEY_STATE`, `KEY_ENABLED`) derived from `STORAGE_KEYS` — correct.
- No bare `AsyncStorage.setItem` with string literals found outside of storage modules.
- Silent `catch {}` in multiple storage modules (onboarding.ts, headlinesSettings.ts, bikeSetupSheet.ts, trackWalk.ts) — consistent pattern but means corrupted data fails silently. No user-visible error path from storage read failures. [P2 — acceptable for read paths, more concerning for write paths]

### Permissions

- Camera: denied path → `Alert` with Settings link ✓ (camera modal).
- Photo library: denied path → `Alert` with Settings link ✓ (bike photo, avatar face).
- Notifications: `requestNotificationPermissions` called before toggling notify ✓.
- Location: `requestForegroundLocationPermission` gated behind track-arrival toggle ✓.

### API URL

- All API URLs defined in `app/constants/api.ts` — no hardcoded URLs found in screen files.
- Default dev path now routes to production Render API (no local `npm start` required for Expo Go testers). Documented in `api.ts` comment.
- `apiFetch` injects `x-app-secret` header when `EXPO_PUBLIC_APP_API_SECRET` is set ✓.

### Offline / API down

- Headlines: `setError` displayed with retry button ✓.
- Calendar: `setError` displayed ✓.
- Coach/Ask: `setSearchError` / `setError` displayed ✓.
- Trivia: `setTriviaLoading(false)` + alert on fail ✓.

### MoMS rule book

- 2026 edition corpus loaded (259,175 chars). `nextReviewDue 2027-01-15` — not overdue. Scope includes Road Race + Historic (full). ✓

---

## Coding improvements

| # | Area | Finding | Priority |
|---|------|---------|----------|
| 1 | Navigation types | `as never` / `as any` casts bypass TypeScript's tab navigation checking in HeadlinesScreen, ImportTrackNotesScreen, BikeSetupBasicsScreen | P1 |
| 2 | Storage key inconsistency | `TRIVIA_BEST_SCORE: 'ROADRACER_TRIVIA_BEST'` breaks `@roadrace_*` prefix convention | P1 |
| 3 | Duplicated avatar pipeline | Avatar pick + camera + align flow duplicated across OnboardingScreen and HeadlinesSettingsScreen (~80 lines each). Consider extracting `useAvatarPicker()` hook | P2 |
| 4 | Screen Brief stale | RiderCoachScreen now hub pattern (not tab bar). BikeBalanceSetupScreen, BikeSetupBasicsScreen, BikeSetupSheetScreen, RoadRacerAiFaqsScreen, CoachChatScreen, and Rules tab in QAScreen not in `SCREEN_BRIEF_FOR_VISUALS.md` | P2 |
| 5 | Silent storage write failures | `catch {}` in write paths (trackWalk.ts, bikeSetupSheet.ts) silently drops data loss. At minimum log to dev console; ideally surface to user | P2 |
| 6 | CalendarScreen duplicate SERIES_COLORS key | `'asbk'` and `'ASBK'` both present — harmless but sloppy | P2 |
| 7 | `onboardingContent.ts` step count | `totalSteps = 8` hardcoded — should derive or assert | P2 |
| 8 | `ImportTrackNotesScreen` does not use `navigateToCoachWithDraft()` | Helper already exists in `rootNavigation.ts`; direct `getParent()` cast is brittle and duplicates logic | P1 |
| 9 | Approximate `~` lengths | `smp_brabham`, `smp_druitt`, `calder_park` use string `~X.X km` format — not numeric; could break length comparisons if added to validator | P2 |
| 10 | `BikeSetupBasicsScreen` `as any` | Line 106 ImagePicker result cast needs proper narrowing | P1 |

---

## Suggested fix order (for Cursor)

1. **[P1] Typed tab navigation — HeadlinesScreen + ImportTrackNotesScreen**: Replace `as never` / `getParent() as ...` with typed `RootTabParamList` navigation or the existing `rootNavigation.navigateToCoachWithDraft()` helper. Eliminates runtime navigation blind spot and deduplicates ImportTrackNotes → Coach handoff.

2. **[P1] Storage key prefix fix**: Rename `TRIVIA_BEST_SCORE` key value from `'ROADRACER_TRIVIA_BEST'` to `'@roadrace_trivia_best'`. Include one-time migration read of the old key in `QAScreen` on first load (read old key, write new key, delete old) so existing user best scores are preserved.

3. **[P1] BikeSetupBasicsScreen `as any` at line 106**: Narrow the ImagePicker result type properly instead of casting. Review what type is being cast and add a type guard.

---

## Manual verify on Expo Go

- [ ] Onboarding: complete 8-step flow on physical device; confirm avatar face camera → Align modal → face bake → home display is correct.
- [ ] Avatar Flip control: camera capture → Align modal → tap Flip → confirm mirrored preview flips correctly.
- [ ] Track arrival: enable in Settings → drive/simulate near a tracked circuit → confirm overlay appears.
- [ ] Coach chat: send a message in `coach` mode and `bikesetup` mode; confirm correct AI persona responds.
- [ ] Track Walk → "Send to Coach": create a corner note, send to coach, confirm RiderCoachTab + CoachChat opens with notes pre-populated.
- [ ] ImportTrackNotes: paste notes, select track, tap "Send to Coach" — confirm navigation to RiderCoachTab.
- [ ] Trivia: play to win (8+ correct), confirm goat explosion; play to fail (3 wrong), confirm fail state. Re-launch app and confirm best score persists.
- [ ] Bike Balance: enter measurements, confirm export report text contains all sections.

---

## Resolved since last review

_(No prior RR_REVIEW to reference — this is the inaugural weekly report.)_

Notable recent fixes already landed on main:
- ✅ Mirror fix: front camera no longer applies mirroring to saved JPEG (`93f97ba`)
- ✅ Flip control added to Align modal for post-capture mirror correction (`a9b91d2`)
- ✅ TrackWalk nested under Coach tools (`cbecc69`)
- ✅ Vercel CORS origins added (`7923159`)
- ✅ BikeBalance variable rename for readability (`5395ed7`)
- ✅ react-native-worklets added for Vercel web build (`409b89d`)

---

## Out of scope

- Visual pixel QA (no iOS/Android simulator access from cron)
- Maestro / detox E2E automation
- GPX bearing analysis (no Desktop GPX package present in cron context)
- Vercel deploy smoke test (not configured in this run)
