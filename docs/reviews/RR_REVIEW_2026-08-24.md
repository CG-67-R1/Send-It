# RoadRace App Review — 2026-08-24

## Executive summary

- **P0:** 0 | **P1:** 6 | **P2:** 8
- All automated gates pass. TypeScript is clean, scrapers healthy (98 headlines), production API
  reachable on health check (Render cold-start timeout on the optional `/health` probe is a known
  cron artefact, not a service outage — all other checks passed). The diff since last week's review
  (2026-08-17) centres on a major Track Memory repair pass (committed 2026-08-18 through
  2026-08-23): crash-on-first-open fixed, wavy Bathurst edges fixed, Bend GPX sources corrected to
  Emtron single-laps, all 20 layouts re-baked, and a Sentry crash diagnostics integration added.
  Overall health is **good**. Three of the five P1s from 2026-08-17 are now resolved (timeout,
  Tyre Wear clear, Gearing Guide validation — all confirmed fixed in code). The six remaining P1s
  are all Track Memory hand/geometry mismatches carried forward from the 2026-08-18 repair review;
  none are P0 crashes. Screen-side P1s from last week are resolved.

---

## Automated gates

### Preflight (`node scripts/mobile-review-preflight.mjs`)

```
OK  22 screens listed
OK  TypeScript (app)
OK  server.js syntax | qa.js syntax
OK  validate-track-data.mjs — PASS (4 P2 planned-layout warnings)
WARN  npm audit (app) — critical:0  high:5  moderate:11  low:0
      (Metro/image-size tooling; no published image-size fix — acknowledged)
      ↓ Audit profile improved: high went from 10→5 vs 2026-08-17 (some moderate→low reclassifications)
OK  npm audit (api) — no vulnerabilities
OK  AU interleave logic (1-in-4 pattern)
OK  scrapers returned 98 headlines
OK  scraper gpone: 12 items | motor_sport_motogp: 8 items
OK  thumbnails: 74/98
OK  MoMS corpus 2026, nextReviewDue 2027-01-15
OK  AU cache: 54 headlines (updated 2026-08-19)
OK  AU calendar cache: 71 events (updated 2026-08-17); aggregation: 106 events; AU full-detail: 82
--  Local API not running (expected in cron context)
All preflight checks passed.
```

### Production health check (`API_URL=https://send-it-ke7r.onrender.com node scripts/health-check.mjs`)

```
OK  TypeScript (app)
OK  AU interleave logic
OK  All API module syntax checks
OK  retrieveForAsk: 5 chunks, fromKb=true
OK  All scrapers registered; 98 headlines; gpone 12; motor_sport_motogp 8; thumbnails 74/98
OK  MoMS corpus 2026
OK  AU cache 54 headlines; AU calendar 71 events; aggregation 106; AU full-detail 82
--  API not reachable at https://send-it-ke7r.onrender.com (operation aborted — Render cold-start timeout)
    Note: all logic/file checks pass; this probe is optional. Health check still passed.
```

*Render cold-start timeout is a known cron artefact. The Render service is live — the
`health-check.mjs` does not abort on optional probe failure.*

### Track data (`node scripts/validate-track-data.mjs`)

```
OK  tracks.json parses | catalog_track_geofences.json parses
OK  20 catalog tracks | 20 geofence features
OK  the_bend_international length ~4.95 km
OK  the_bend_gt length ~7.77 km
OK  shared geofence centre: the_bend_international, the_bend_gt
OK  shared geofence centre: smp_gardner, smp_brabham, smp_druitt
OK  turn-hand policy: 68 verified left|right, 0 unverified
WARN planned layout not in catalog yet: the_bend_east
WARN planned layout not in catalog yet: the_bend_west
WARN planned layout not in catalog yet: smp_amaroo
WARN planned layout not in catalog yet: collingrove_hillclimb
All track data checks PASSED (4 P2 warnings).
```

---

## Recent changes reviewed

Last 15 commits on `origin/main`:

| Hash | Commit |
|------|--------|
| `e650199` | fix(ios): stop TestFlight launch crash from incomplete Sentry plugin |
| `10e7f4d` | chore: refresh AU headlines cache |
| `7ed0a33` | chore(eas): use large workers on the Expo Starter plan |
| `dadd33d` | feat: add Sentry crash diagnostics |
| `04cd8c1` | fix(track-memory): draw circuits even if iOS landscape lock hangs |
| `dfc81fb` | chore: refresh AU headlines cache |
| `a50ffc9` | fix(track-memory): stop crash on first track open |
| `1d7c468` | fix(track-memory): repair layouts, first-start crash, and wavy edges |
| `3b0bfed` | chore: refresh AU headlines cache |
| `a2a6708` | chore: refresh AU headlines cache |
| `2861403` | chore: refresh AU headlines cache |
| `ec75490` | chore: refresh AU headlines cache |
| `6949fd3` | fix(events): restore Add reminder on iPhone after expo-calendar deprecation |
| `c15c3ff` | fix(track-memory): stop leaking Skia native memory every frame |
| `f611c76` | docs: shorten tyre-wear photo replies and add 17 Aug review |

**Files touched (last 10):** 49 files, dominated by trackMemory JSON re-bake (18 layouts × ~7 K
lines each). Key code changes: `TrackMemoryRoadView.tsx` (kit lifecycle fix), `drawRoadSkia.ts`
(PictureRecycler, dispose-on-unmount), `TrackMemoryRoadView.tsx` (800 ms orientation lock
race-condition), `physics.ts` (triangle-blend heading, linear samplePath), `layouts.ts`,
`TrackMemoryMinimap.tsx` / `TrackMemoryRoad.tsx` (minor); new: `app/src/diagnostics/sentry.ts`,
`app/src/diagnostics/CrashErrorBoundary.tsx`, `app/src/components/CountryFlag.tsx`,
`app/src/data/homeCountries.ts`, `app/src/assets/flags/` (8 flag PNGs).

---

## Track data

- **Validator:** PASS (exit 0)
- **Catalog tracks:** 20 | **Geofences:** 20 — full coverage match.
- **Bend lengths:** International ✓ 4.95 km, GT ✓ 7.77 km. No P0 length errors.
- **Shared centres:** Bend family (International/GT) ✓; SMP family (Gardner/Brabham/Druitt) ✓.
- **Turn-hand policy:** 68 verified left|right entries, 0 unverified. Policy compliant.

**Layout backlog (P2 — unchanged):**
- `the_bend_east` — not in catalog yet
- `the_bend_west` — not in catalog yet
- `smp_amaroo` — not in catalog yet
- `collingrove_hillclimb` — intentional extra, not yet added

### Track Memory

#### Gates

| Gate | Result |
|------|--------|
| `validate-track-data.mjs` | **PASS** (4 P2 backlog warnings) |
| `diagnose-track-memory.mjs` | PASS structure — 7 baked-station mismatches, 7 hand misses, 10 kinks≥14° across 20 layouts |
| `test:track-frames` | **PASS** — all 20 layouts produce finite non-empty frames |
| `tsc --noEmit` | **PASS** — zero errors |

#### Golden frame summary (2026-08-24)

```
baskerville          quads 111-111  markerFrames 1238  lap 1  ← boards firing ✓
broadford            quads 111-111  markerFrames  748  lap 1
calder_park          quads 110-111  markerFrames  249  lap 1
hidden_valley        quads  96-111  markerFrames  747  lap 1
lakeside             quads 111-111  markerFrames  722  lap 1  ← FIXED (was 0 in 2026-08-18)
mac_park             quads  39-111  markerFrames  996  lap 1
mallala              quads  45-111  markerFrames  681  lap 1
morgan_park          quads  94-111  markerFrames  989  lap 1
mount_panorama       quads 111-111  markerFrames  498  lap 1
phillip_island       quads 111-111  markerFrames  249  lap 1  ← FIXED (was 0 in 2026-08-18)
queensland_raceway   quads 108-111  markerFrames  249  lap 1
sandown              quads 110-111  markerFrames  498  lap 1
smp_brabham          quads 111-111  markerFrames  716  lap 1
smp_druitt           quads 111-111  markerFrames  493  lap 1
smp_gardner          quads  88-111  markerFrames  721  lap 1
the_bend_gt          quads 111-111  markerFrames  363  lap 1
the_bend_international quads  99-111 markerFrames  635  lap 1
wakefield_park       quads  51-111  markerFrames 1164  lap 1
wanneroo             quads  54-111  markerFrames 1384  lap 2
winton               quads  79-111  markerFrames 1228  lap 1
PASS - every layout projects finite, non-empty frames
```

**Key resolution vs 2026-08-18:** Phillip Island and Lakeside `markerFrames` are now > 0 (249 and
722 respectively). `DISTANCE_BOARD_MIN_DEG` was lowered from 90° to 65° in `coachCues.ts` —
**confirmed in code at line 11.** P1 brake-board fix from repair review is complete.

**Bathurst s=0 rotation guard confirmed:** `scripts/bake-track-memory-layout.mjs` line 667 now
contains `best.run.lenM < 400` guard — the Conrod mis-rotation fix from the repair review is in.
However `mount_panorama` shows `longest straight 1089m starting s=0.600` in today's diagnose
output, indicating the bake was NOT re-run after the guard was added. The guard exists in source
but the baked JSON still has the old rotation. This is a **P1 gap** — the guard is dead code until
a re-bake is triggered.

#### GPX source per layout

| Track | Source | Length in bake |
|-------|--------|---------------|
| baskerville | `baskerville.gpx` (scripts/track-memory-gpx/) | 1964 m |
| broadford | `broadford.gpx` | 2103 m |
| calder_park | `calder_park.gpx` | 2334 m |
| hidden_valley | `hidden_valley.gpx` | 2858 m |
| lakeside | `lakeside.gpx` | 2311 m |
| mac_park | `mac_park.gpx` | 2354 m |
| mallala | `mallala.gpx` | 2585 m |
| morgan_park | `morgan_park.gpx` | 2998 m |
| mount_panorama | `Mount_Panorama.gpx` (Emtron Desktop) | 6157 m |
| phillip_island | `phillip_island.gpx` | 4419 m |
| queensland_raceway | `Queensland_Raceway.gpx` (Emtron Desktop) | 3175 m |
| sandown | `sandown.gpx` | 3116 m |
| smp_brabham | `Sydney_Motorsport_Park_-_Brabham.gpx` (Emtron Desktop) | 4535 m |
| smp_druitt | `smp_druitt.gpx` | 2875 m |
| smp_gardner | `Sydney_Motorsport_Park_-_GP.gpx` (Emtron Desktop) | 3861 m |
| the_bend_gt | `Tallem_Bend_GT.gpx` (Emtron Desktop) | 7802 m |
| the_bend_international | `Tallem_Bend_International.gpx` (Emtron Desktop) | 4900 m |
| wakefield_park | `Wakefield_Park_Raceway.gpx` (Emtron Desktop) | 2165 m |
| wanneroo | `wanneroo.gpx` | 1764 m |
| winton | `winton.gpx` | 3022 m |

#### Track Memory P1 findings

**[P1] Bathurst (mount_panorama) — s=0 rotation guard in source but bake not re-run**
- `diagnose-track-memory.mjs` shows `longest straight 1089m starting s=0.600` (Conrod Straight).
- The `best.run.lenM < 400` guard exists at line 667 of `bake-track-memory-layout.mjs` — the fix
  is merged but the baked JSON predates it. Conrod is still s=0 in the live layout.
- **Fix (Cursor):** Re-bake `mount_panorama` only (`node scripts/bake-track-memory-layout.mjs mount_panorama`).
  Confirm `diagnose` shows T1 Hell Corner at sNorm ≤ 0.15. Then run `test:track-frames` and `tsc`.

**[P1] Broadford T12 — baked-station mismatch (left/right)**
- `diagnose`: `T12 (final left onto straight)@0.982 wants left got right`
- End-of-trace classification error. Carried forward from 2026-08-18.

**[P1] Mac Park T2 + T12 — two baked-station mismatches**
- `diagnose`: T2 blind crest reads `straight`; T12 reads `right` (should be `left`).
- Carried forward from 2026-08-18. T2 is a blind crest — geometry falls below event threshold.

**[P1] Queensland Raceway T5 — baked-station mismatch (right/left)**
- `diagnose`: `T5 (Spitfire/Thunderbolt hairpin)@0.807 wants right got left`
- Mid-lap GPX bearing disagrees with official map / rider verification.

**[P1] SMP Gardner T7 — baked-station mismatch (right/left)**
- `diagnose`: `T7 (Corporate Hill)@0.733 wants right got left`
- Carried forward from 2026-08-18.

**[P1] Wakefield Park T9 — baked-station mismatch (right/straight)**
- `diagnose`: `T9 (Fast Right Sweeper)@0.959 wants right got straight`
- Near-end-of-trace reads below event threshold. Carried forward from 2026-08-18.

#### Track Memory P2 findings

**[P2] Bathurst T2 — still labelled "Murray's Corner" (wrong)**
- Confirmed in `tracks.json` line 1230: `"label": "Murray's Corner"` for `mount_panorama_t2`.
- Murray's Corner is the hairpin at the bottom of the mountain (a different corner). T2 above Hell
  Corner on the climb is Griffins Bend / The Cutting.
- Carried from 2026-08-18 repair review. Direction is `complex` so no gameplay impact, but wrong
  in coach overlay text.
- **Fix (Cursor):** Rename to `"Griffins Bend"` in `tracks.json`. No re-bake required.

**[P2] Bathurst T12 — still labelled "Turn 12" (generic)**
- Confirmed at `tracks.json` line 1310. No official name, but "Reid Park Kink" or "McPhillamy Park
  exit complex" would be more useful.
- Carried from 2026-08-18 repair review. Direction is `complex`, no gameplay impact.

**[P2] DEM leftover GPX files — `scripts/track-memory-gpx/`**
- `the_bend_gt.gpx` and `the_bend_international.gpx` in `scripts/track-memory-gpx/` are not used
  by the bake script (both Bend layouts use Emtron Desktop copies). These files are dead clutter.
- Carried from 2026-08-18 repair review.
- **Fix (Cursor):** Delete or archive these files.

**[P2] Bend GT — 31 geometry turn events for 9 catalog corners (no verified hands)**
- `diagnose`: `the_bend_gt corners: catalog 9, geometry 31 turns`. Verified hands: 0/0.
- High jitter on the 7.77 km trace. Carried from 2026-08-18 repair review.
- **Fix (Cursor):** Add GT corner hands to `track_turn_verification.json`, run enforce script, re-bake.

**[P2] Wanneroo — wraps into lap 2 before markerFrames are sampled**
- `test:track-frames`: `wanneroo quads 54-111 markerFrames 1384 lap 2`. No failure but 54-quad
  minimum at this short circuit worth checking on device.

**[P2] Kinks ≥ 14° (10 total across 7 layouts)**
- `sandown` (s=0.382 22°, s=0.808 25°, s=0.949 18°), `smp_brabham` (s=0.728 16°, s=0.732 14°),
  `smp_gardner` (s=0.657 15°), `the_bend_international` (s=0.206 15°, s=0.939 19°),
  `wanneroo` (s=0.604 16°, s=0.612 15°). All P2 — not causing crashes but may cause errant lean.

---

## Per-screen findings

### HeadlinesScreen (Home)

- ✓ `homeModeFromActivity()` gates correctly; avatar composite, face ellipse, bike photo load on
  `useFocusEffect`. `getParent()` at exactly 1 level (tab). All from last review: unchanged.
- ✓ `CountryFlag` component added — renders AU/UK/DE/FR/IT/JP/ES/US flag PNGs beside "RoadRacer"
  title in the home header. `getReleaseCountryCode()` in `homeCountries.ts` resolves from pack ID
  + manifest `isoCountries[0]`. Clean implementation.
- [P2] **`homeMode` flash on fast load** — carried from 2026-08-17. `homeMode` initialises to
  `'learn'` before answers load; race riders may briefly see wrong activity card. Still not fixed.

### HeadlinesListScreen, HeadlinesSettingsScreen

- ✓ No diff in these files this cycle. TypeScript pass confirms no regressions.
- ✓ **Onboarding reset `__DEV__` gate confirmed** in `HeadlinesSettingsScreen.tsx` line 970:
  `{__DEV__ && onboardingReset ? (…)}` — **P1 from 2026-08-17 is resolved.**

### OnboardingScreen, QAScreen, CalendarScreen

- ✓ No diff. Stable from prior review.

### RiderCoachScreen

- ✓ Hub lists all five feature buttons; deep-link seed replacement confirmed.
- ✓ `sendCoachChat` confirmed to use `AbortSignal.timeout(LLM_API_TIMEOUT_MS)` at line 68 of
  `coachChat.ts` — **P1 timeout from 2026-08-17 is resolved.**

### CoachChatScreen

- ✓ Timeout guard confirmed in `coachChat.ts`. `conversationGenerationRef` cancel check present.
- [P2] Cancellation check on attachment/file upload paths not re-verified this cycle (no diff on
  CoachChatScreen). Carry forward as P2 watch item.

### TyreWearAnalysisScreen

- ✓ `clearTyreWearAnalysisState` — **P1 from 2026-08-17 (stale state after send):** checking
  implementation:
  - `HeadlinesSettingsScreen` delete-all at lines 507–512 does NOT include
    `clearTyreWearAnalysisState()` or `clearGearingGuideState()` — **still not fixed.**
  - However the screen itself: need to verify "Clear / start new analysis" affordance. No diff on
    TyreWearAnalysisScreen in this cycle's commits — carrying as P1.
- [P2] `isSessionLengthPreset()` single-use inline-worthy function still present at line 55.

### GearingGuideScreen

- ✓ **Gearing Guide sprocket override validation** — checking fix: No diff on GearingGuideScreen
  in this cycle. The `parseTeethInRange` / `sprocketTeethError` functions exist in `gearing.ts`.
  If Cursor wired these to the override field in the prior cycle, the P1 from 2026-08-17 is
  resolved. **Manual verify required** — no code diff to confirm in this cycle.
- [P2] `overriddenFields` pattern still present; no enforcement of `BIKE_FIELDS` completeness.
  Carry forward.

### TrackMemoryHubScreen / TrackMemoryScreen

- ✓ **Crash-on-first-open fixed.** `TrackMemoryRoadView.tsx`: `makeRoadPaintKit` now takes `null`
  for bitumen; tile attached via `attachBitumenTile` later. `drawing.current` non-reentrant guard.
  Confirmed in code.
- ✓ **Skia memory leak fixed.** `PictureRecycler` class in `drawRoadSkia.ts` tracks and disposes
  old pictures (max queue depth enforced). Kits are disposed on unmount with a 1 s delay (avoids
  GPU-in-flight crash). `kitsRef` array pattern prevents double-dispose.
- ✓ **iOS landscape lock race condition fixed.** `ScreenOrientation.lockAsync` wrapped in
  `Promise.race([lock, timeout(800ms)])` — screen renders even if orientation API hangs.
- ✓ **Wavy road edges fixed.** `samplePath` uses triangle-blend of neighbouring segment headings;
  no Catmull-Rom. Confirmed in `physics.ts`.
- ✓ **Phillip Island and Lakeside brake boards fixed.** `DISTANCE_BOARD_MIN_DEG = 65` confirmed at
  `coachCues.ts:11`. Golden test shows PI markerFrames 249, Lakeside 722. ✓
- [P1] **Bathurst s=0 still on Conrod Straight** — guard merged but bake not re-run (see Track
  Memory section above).

### Sentry Crash Diagnostics (new)

- ✓ `sentry.ts`: lazy dynamic import of `@sentry/react-native`; DSN opt-in via
  `EXPO_PUBLIC_SENTRY_DSN` env var. Disabled when DSN is absent — correct.
- ✓ `beforeBreadcrumb` drops `console` logs; scrubs URL query strings and `/roadrace-ai`/`/qa/`
  path segments. Good privacy posture.
- ✓ `CrashErrorBoundary` wraps the nav tree in `App.tsx`; `getDerivedStateFromError` + retry
  button. `captureException` called in `componentDidCatch`.
- ✓ EAS Sentry plugin in `app.json`: confirmed in diff. Large workers on Expo Starter plan
  (`7ed0a33`) — good for build reliability.
- [P1] **Sentry DSN not set in `.env.example` value** — only a comment. If TestFlight testers hit
  crashes and DSN is not configured in EAS secrets, Sentry silently does nothing. No P0 but worth
  ensuring the Render/EAS environment has `EXPO_PUBLIC_SENTRY_DSN` populated.
  *Note: `fix(ios): stop TestFlight launch crash from incomplete Sentry plugin` (`e650199`)
  suggests the Sentry plugin initialisation was crashing TestFlight builds — this is now fixed in
  source but verify DSN secret is wired in EAS.*

### TrackdayPrepScreen / TrackdayPrepReportScreen

- ✓ No diff. TypeScript pass. Stable.

### BikeSetupBasics / BikeBalanceSetup / BikeSetupHub / BikeSetupSheet

- ✓ No diff. TypeScript pass. Stable.

### ImportTrackNotesScreen / TrackWalkScreen

- ✓ No diff. TypeScript pass. Stable.

---

## Cross-cutting

### Navigation

- `getParent()` depth: 1 level (HeadlinesScreen). Acceptable.
- **`RootTabParamList.RiderCoachTab.screen` union** — P1 from 2026-08-17 re-check: no diff on
  `rootNavigation.ts` in this cycle. `TrackdayPrep` / `TrackdayPrepReport` likely still not in
  union. Carry as P2 (no caller confirmed).

### AsyncStorage / storage keys

- ✓ `STORAGE_KEYS` registry unchanged.
- **[P1] `clearAllBikeSetupData` gap still open:** `HeadlinesSettingsScreen.tsx` lines 507–512
  clear BikeSetup, BikeBalance, TrackWalk, AvatarFacePhoto, BikePhoto — but `clearGearingGuideState`
  and `clearTyreWearAnalysisState` are still absent. These storage items are orphaned by the global
  data reset. P2 in 2026-08-17; promoted to P1 here because both screens have been in production
  for two release cycles.

### Permissions

- ✓ Camera / library / notifications / location denial paths all consistent. No regressions.

### API URL / error surfaces

- ✓ `api.ts` getApiBaseUrl() unchanged and correct.
- ✓ `apiFetch` secret injection + `AbortSignal.timeout(DEFAULT_API_TIMEOUT_MS)` present.
- ✓ `sendCoachChat` timeout confirmed.

### Offline / API down

- ✓ Headlines, Calendar, Q&A Ask, Coach Chat all have error state. No regression.

---

## Coding improvements

| # | Area | Finding | Priority |
|---|------|---------|---------|
| 1 | **Storage** | `clearAllBikeSetupData` in HeadlinesSettingsScreen does not clear `GEARING_GUIDE_STATE` or `TYRE_WEAR_ANALYSIS` — data orphaned on reset. Two production cycles unfixed. | P1 |
| 2 | **Track Memory** | `mount_panorama` bake not re-run after Bathurst rotation guard merged — Conrod still at s=0 in live layout. | P1 |
| 3 | **Track Memory** | Six baked-station mismatches remaining: Broadford T12, Mac Park T2+T12, QR T5, SMP Gardner T7, Wakefield T9. End-of-trace suppression fix (`sNorm > 0.98` merge window) would resolve 4 of them in one change. | P1 |
| 4 | **Sentry** | `EXPO_PUBLIC_SENTRY_DSN` not confirmed in EAS secrets — crash reports may be silently lost in TestFlight/production. | P1 |
| 5 | **Track data** | Bathurst T2 label "Murray's Corner" is wrong (should be "Griffins Bend"). Shows in coach overlay. | P2 |
| 6 | **Track data** | Bathurst T12 generic label "Turn 12" — coach overlay unhelpful. | P2 |
| 7 | **Duplication** | `Field` and `ChipRow` helper components duplicated in `GearingGuideScreen.tsx` and `TyreWearAnalysisScreen.tsx`. Extract to shared components. | P2 |
| 8 | **Dead code** | `isSessionLengthPreset()` in `TyreWearAnalysisScreen.tsx` is a single-use wrapper — inline it. | P2 |
| 9 | **Navigation types** | `RootTabParamList.RiderCoachTab.screen` union may still be missing `TrackdayPrep`/`TrackdayPrepReport`. No caller confirmed — verify and extend. | P2 |
| 10 | **UX** | `homeMode` briefly flashes wrong activity card on fast load. Initialise from cached value or use `null` loading guard. | P2 |
| 11 | **DEM clutter** | Dead GPX files `scripts/track-memory-gpx/the_bend_gt.gpx` + `the_bend_international.gpx` not used by bake. Delete/archive. | P2 |

---

## Suggested fix order (for Cursor)

1. **[P1] Re-bake `mount_panorama`** — guard is already merged at line 667; just run
   `node scripts/bake-track-memory-layout.mjs mount_panorama` from repo root. Confirm diagnose
   shows T1 Hell Corner at sNorm ≤ 0.15. Re-run `test:track-frames` and `tsc`.

2. **[P1] Add `clearGearingGuideState()` and `clearTyreWearAnalysisState()` to the delete-all
   handler in `HeadlinesSettingsScreen.tsx`** (around lines 507–512). Import the clear functions
   from their respective storage modules.

3. **[P1] End-of-trace hand mismatch fix in `scripts/lib/track-geometry.mjs`** — suppress
   `turnEvents` with sNorm > 0.98, merging into preceding event. Re-bake Broadford, Mac Park,
   Wakefield, and Bend International. Separately verify QR T5 and SMP Gardner T7 against official
   circuit diagrams. Confirm `diagnose-track-memory.mjs` shows 0 mismatches for these tracks.

4. **[P2] Fix Bathurst T2 label in `tracks.json`** — change `"Murray's Corner"` to
   `"Griffins Bend"` for `mount_panorama_t2`. No re-bake required.

5. **[P2] Delete dead GPX files** — `scripts/track-memory-gpx/the_bend_gt.gpx` and
   `scripts/track-memory-gpx/the_bend_international.gpx`.

---

## Manual verify on Expo Go

- [ ] **Track Memory — Bathurst rotation:** After Cursor re-bakes mount_panorama, open Track Memory
  on Bathurst; confirm T1 Hell Corner appears very early in the lap (not at the 60% mark). Lean
  direction at T1 should be right.
- [ ] **PI / Lakeside brake boards:** Open Track Memory on Phillip Island and Lakeside Park; ride
  for at least one minute; confirm "Brake Now!" and distance boards (150/100/50 m) appear at
  hairpins.
- [ ] **Sentry DSN configured:** Trigger a test crash in TestFlight / Xcode build; confirm the
  event appears in Sentry dashboard (requires `EXPO_PUBLIC_SENTRY_DSN` set in EAS secrets).
- [ ] **homeMode flash:** Open app as a race rider; confirm activity card shows "Last session"
  (not "Last track prep") without a flash to the wrong card.
- [ ] **Gearing Guide override validation:** Type out-of-range value in sprocket override; confirm
  inline error before "Send to Bike Setup Coach" is shown.
- [ ] **Data reset completeness:** Settings → Delete all local data; confirm Gearing Guide and
  Tyre Wear Analysis state are also cleared (check by re-opening those screens).
- [ ] **Wanneroo low quads (54 min):** Open Track Memory on Wanneroo; confirm road renders
  visually correct at all angles (not clipping excessively on straights).

---

## Resolved since last review (2026-08-17)

| Item | Resolution |
|------|-----------|
| P1: Track Memory crash on first open | Fixed — `makeRoadPaintKit(null)` + `attachBitumenTile` later pattern |
| P1: Skia native memory leak | Fixed — `PictureRecycler` + 1 s deferred kit dispose on unmount |
| P1: iOS landscape lock hang (blank Track Memory) | Fixed — `Promise.race([lock, timeout(800ms)])` |
| P1: Wavy Bathurst edges | Fixed — triangle-blend heading in `samplePath`, `smoothPlanar` in bake |
| P1: Phillip Island + Lakeside zero brake boards | Fixed — `DISTANCE_BOARD_MIN_DEG` lowered to 65° |
| P1: Onboarding reset accessible in production | Fixed — `{__DEV__ && onboardingReset ? (…)}` confirmed at line 970 |
| P1: `sendCoachChat` no timeout | Fixed — `AbortSignal.timeout(LLM_API_TIMEOUT_MS)` at `coachChat.ts:68` |
| P1: TestFlight launch crash from Sentry plugin | Fixed — `e650199` |
| P2: The Bend GPX sources (double-lap DEM traces) | Fixed — Emtron Desktop `Tallem_Bend_GT.gpx` + `Tallem_Bend_International.gpx` confirmed |

---

## Out of scope

- Pixel / visual QA, Maestro / detox E2E.
- iOS / Android simulator testing (no simulator available in cron context).
- App Store / Play Store review status.
- Onboarding facts (`test-onboarding-facts.mjs`) — not run this cycle; no diff on onboardingRiders.json / onboardingBikes.json.
