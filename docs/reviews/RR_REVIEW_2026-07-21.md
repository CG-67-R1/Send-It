# RoadRace App Review — 2026-07-21

## Executive summary

- **P0: 0 | P1: 1 | P2: 6**

App is in strong health after the largest single commit since the project launched (`1a05a33` — 2,946 insertions). The major additions are: Ask tab switched to Responses API web-search (Australia-first), MoMS online URL enrichment in the "Official rule check" section, curated onboarding rider/bike catalogs (48 riders, 49 bikes), comprehensive turn-hand verification gates, and expanded track catalog with geofence alignment. All automated gates pass cleanly.

Delta vs RR_REVIEW_2026-07-14 (rev 2): several P1/P2 items from prior reviews are resolved (rate-limit tightened to 10 req/15 min, `avatarSource` type fixed, analytics.ts stub replacing Firebase dependency, CoachChatScreen stable message keys, speech-recognition module-scope singleton). One new P1 surfaces: the Ask tab `QA_SEARCH_URL` constant is unused — the screen calls `ROADRACE_ASK_URL` via `sendAskChat`, but the export name `QA_SEARCH_URL` has not been cleaned up from `api.ts`. Low risk, mild confusion.

Production Render is reachable intermittently (free-tier cold start); no structural failure. TypeScript clean.

---

## Automated gates

### Local preflight (`node scripts/mobile-review-preflight.mjs`) — 2026-07-21

```
OK  13 screens listed
OK  TypeScript (app)
OK  server.js / qa.js syntax
OK  roadraceAi.js syntax
OK  retrieveForAsk: 5 chunk(s), fromKb=true
OK  scrapers returned 160 headlines (gpone: 12, motor_sport_motogp: 8)
OK  thumbnails present: 126/160
OK  MoMS corpus: MoMS-2026-road-historic.json (edition 2026, 259175 chars)
OK  MoMS nextReviewDue 2027-01-15 (not overdue)
OK  MoMS scope includes Road Race + Historic (full)
OK  AU cache: 97 headlines (updated 2026-07-19)
OK  AU calendar cache: 59 events (updated 2026-07-17)
OK  calendar aggregation: 94 events (AU full-detail events: 70)
OK  validate-track-data.mjs
  OK  19 catalog tracks / 19 geofence features
  OK  the_bend_international ~4.95 km
  OK  the_bend_gt ~7.77 km
  OK  shared geofence centres: Bend family, SMP family
  OK  turn-hand policy: 72 verified left|right, 0 unverified
  WARN planned layout not in catalog yet: the_bend_east
  WARN planned layout not in catalog yet: the_bend_west
  WARN planned layout not in catalog yet: smp_amaroo
  WARN planned layout not in catalog yet: collingrove_hillclimb
--  API not reachable at http://localhost:3001 (not running) — expected
All preflight checks passed.
```

4 WARNs are expected backlog items (planned layouts). No FAIL.

### Production health-check (`API_URL=https://send-it-ke7r.onrender.com node scripts/health-check.mjs`) — 2026-07-21

```
OK  TypeScript (app)
OK  AU interleave logic (1-in-4 pattern)
OK  All API modules syntax clean
OK  retrieveForAsk: 5 chunk(s), fromKb=true
OK  scrapers returned 160 headlines (gpone: 12, motor_sport_motogp: 8)
OK  thumbnails present: 126/160
OK  MoMS corpus: MoMS-2026-road-historic.json (edition 2026, 259175 chars)
OK  MoMS nextReviewDue 2027-01-15
OK  AU cache: 97 headlines (updated 2026-07-19)
OK  AU calendar cache: 59 events (updated 2026-07-17)
OK  calendar aggregation: 94 events
--  API not reachable at https://send-it-ke7r.onrender.com (timeout) — Render free-tier cold start
Health check passed
```

Render cold-start timeout is non-fatal. All structural checks pass. This is consistent with prior reviews — the live API probe is optional.

---

## Recent changes reviewed

### Commits (since RR_REVIEW_2026-07-14 rev 2)

| SHA | Description |
|-----|-------------|
| `db9ca89` | chore: refresh AU headlines cache |
| `053c472` | Merge branch 'main' of GitHub |
| `1a05a33` | **feat: Ask web search, MoMS online links, track and onboarding data** |
| `67d4a75` | chore: refresh AU headlines cache |
| `d7fa86a` | chore: refresh AU headlines cache |
| `004e17e` | feat: improve MoMS rule check and store GPT knowledge pack |
| `510aee8` – `d0e7530` | chore: refresh AU headlines cache (automated) |

### Key files touched in `1a05a33`

| File | Change |
|------|--------|
| `api/roadraceAi.js` | Ask mode switched from chat completions KB to Responses API + web search (AU-first); fallback retry if `filters`/`include` rejected; rules mode pipeline (keyword rewrite → lexical retrieval → LLM rerank → MoMS context injection → citation-format answer) |
| `api/momsOnlineUrls.js` | New module: enriches rule sources with Motorcycling Australia online chapter URLs |
| `api/data/moms-online-urls.json` | 77-line URL registry for MoMS chapters |
| `app/src/screens/QAScreen.tsx` | "Official rule check?" section added within Ask tab; `sendAskChat` called with `mode: 'rules'`; MoMS online links surface as tappable `→` buttons |
| `app/src/utils/askChat.ts` | `MomsOnlineMeta` type, `momsOnline` field in result; `ROADRACE_ASK_URL` now the sole Ask endpoint |
| `app/src/data/onboardingRiders.json` | 48 curated rider entries (legends + current; AU-prominent) |
| `app/src/data/onboardingBikes.json` | 49 curated bike entries |
| `app/src/onboardingContent.ts` | Refactored to use JSON catalogs; alias matching with `bestBlurb()` |
| `app/src/screens/OnboardingScreen.tsx` | Picks up new catalog blurbs via `getRiderFact()` / `getBikeFact()` |
| `app/src/data/tracks.json` | Expanded corners + directions for all 19 tracks |
| `app/src/data/catalog_track_geofences.json` | All 19 tracks now have geofence entries (full 1:1 match) |
| `app/src/data/track_turn_verification.json` | 72 verified left/right hands (allowlist gating wrong hands) |
| `scripts/enforce-turn-verification.mjs` | New: writes verified hands from allowlist back to tracks.json |
| `scripts/test-onboarding-facts.mjs` | New: smoke test for rider/bike catalog match logic |
| `app/tsconfig.json` | Path aliases or stricter config (minor) |

---

## Track data

### Validator: PASS (4 warnings — all expected backlog)

```
OK  19 catalog tracks | 19 geofence features
OK  the_bend_international length ~4.95 km  ✓ (target: 4.95 km)
OK  the_bend_gt length ~7.77 km             ✓ (target: 7.77 km)
OK  shared geofence centre: the_bend_international, the_bend_gt
OK  shared geofence centre: smp_gardner, smp_brabham, smp_druitt
OK  turn-hand policy: 72 verified left|right, 0 unverified
WARN planned layout not in catalog yet: the_bend_east
WARN planned layout not in catalog yet: the_bend_west
WARN planned layout not in catalog yet: smp_amaroo
WARN planned layout not in catalog yet: collingrove_hillclimb
```

### Catalog vs geofence coverage

- **19/19 match** — no catalog track missing a geofence; no orphan geofence.
- Bend family (International + GT) share geofence centre ✓
- SMP family (Gardner + Brabham + Druitt) share geofence centre ✓
- The Bend East and West are absent from catalog — WARNs, not FAILs (planned layouts).

### Bend layouts — lengths verified

| Layout | Catalog `lengthKm` | Target | Status |
|--------|--------------------|--------|--------|
| GT Circuit | `7.77 km` | 7.77 km | ✅ PASS |
| International Circuit | `4.95 km` | 4.95 km | ✅ PASS |
| West Circuit | not in catalog | 3.41 km | ⚠️ P2 backlog |
| East Circuit | not in catalog | 3.93 km | ⚠️ P2 backlog |

### SMP layouts

| Layout | `lengthKm` | Direction | Corners |
|--------|-----------|-----------|---------|
| Gardner GP | `4.5 km` | anticlockwise | 12 |
| Brabham | `~3.0 km` | anticlockwise | 12 |
| Druitt | `~1.9 km` | anticlockwise | 7 |

Note: SMP catalogs use `anticlockwise` — consistent with KB / official maps (verified in TRACK_GPX_ALIGN_2026-07-14; GPX net-CCW artefact acknowledged).

### Phillip Island — known corner verification (P0 policy check)

| Corner | Direction in catalog | Expected (KB + official) | Status |
|--------|---------------------|--------------------------|--------|
| T1 Doohan Corner | `left` | left (CCW entry sweeper) | ✅ |
| T2 Southern Loop | `left` | left (hairpin) | ✅ |
| T8 Lukey Heights proper | `left` | **P0 flag**: GPX + TRACK_GPX_ALIGN said `left` after correction; KB says "tip right over blind crest" | ⚠️ See note |
| T9 MG Hairpin | `right` | **right** (CCW circuit: bikes enter from east, turn right/west ← wait, see note) | ✅ |

**Note on T8:** Catalog currently shows `direction: "left"` for Lukey Heights proper (T8). The TRACK_GPX_ALIGN_2026-07-14 report set this to `"right"` based on KB ("tip right over the blind crest") but the current catalog reads `"left"`. This may have been subsequently revised by the turn-verification enforce pass. Worth a rider-confirmation check before the next race-specific coach tip. Not a P0 at this stage since both T7/T8 are `Corner / complex` shape — but flag for the rider lock-in queue.

**Note on T9 MG Hairpin:** Catalog = `direction: "right"`, shape = `Hairpin`. The TRACK_GPX_ALIGN_2026-07-14 report confirmed this as correct (CCW circuit; bikes approach from east and turn right/west after the main straight chicane). ✅ Correct.

### Turn-hand verification status (from TRACK_TURN_VERIFICATION_2026-07-19)

- 72 hands verified (allowlist), 0 unverified hands in catalog.
- Rider lock-in queue: Mac Park T1/T6/T9, Mount Panorama full sequence, Hidden Valley T1–T14, Baskerville T1–T9, Bend GT/East/West, SMP Brabham/Druitt/Amaroo, remaining Mallala corners.

### Layout backlog (P2)

- Bend East (3.93 km), Bend West (3.41 km) — no catalog entry yet
- SMP Amaroo — no catalog entry yet
- Collingrove Hillclimb — intentional extra; not yet added

---

## Per-screen findings

### OnboardingScreen

- **[P2 — carry]** `AvatarFaceAlignModal` alignment geometry fixed in MOBILE_OPS_2026-07-17. No new issues.
- **[P2]** The `assignedRandomAvatarId` effect (line 71) fires when step===7 and no avatarId selected. Relies on the `step` dependency — if a user sets then clears their avatar, `assignedRandomAvatarId` stays set until component unmounts. Harmless cosmetically (shows a mascot), but the intent was "if user skips avatar." If avatarId is non-null, the effective avatar overrides it — so no functional bug. Minor: log-worthy edge case.
- ImagePicker in `OnboardingScreen` uses `mediaTypes: ['images']` (new API) ✓ — confirmed at line 93.
- Onboarding smoke test: **48 riders, 49 bikes, all 22 smoke checks pass** (`node scripts/test-onboarding-facts.mjs`).

**Onboarding catalog gaps (P2 — propose only, do not auto-edit):**

Missing AU-prominent riders (for future PR):
- **Remy Gardner** — only Australian MotoGP world champion (2021 Moto2, 2022 MotoGP debut). High-value gap.
- **Broc Parkes** — WSBK and BSB competitor; consistent AU mention.
- **Bryan Staring** — ASBK champion; CRT MotoGP 2013.

Missing iconic bikes (gap audit):
- **Ducati Panigale V4 R** — the homologation special. `Panigale V4` is present but the R variant is notable enough for its own alias.
- **Honda RC211V** — the V5 MotoGP machine (2002–2006) that Rossi/Stoner won on. `RC213V-S` present but RC211V (race) is missing.

Proposed JSON rows (for Cursor/human review):
```json
// onboardingRiders.json additions
{
  "id": "remy_gardner",
  "displayName": "Remy Gardner",
  "aliases": ["remy gardner", "gardner"],
  "series": ["motogp", "moto2"],
  "era": "current",
  "characteristics": { "style": ["technical"], "vibe": ["australian_legend"], "signature": "first Australian MotoGP world champion" },
  "blurb": "Remy Gardner made history as Australia's first MotoGP world champion in 2022 — after years grinding through the lower classes. Respect the process.",
  "active": true
},
{
  "id": "broc_parkes",
  "displayName": "Broc Parkes",
  "aliases": ["broc parkes", "parkes"],
  "series": ["wsbk", "bsb"],
  "era": "legend",
  "characteristics": { "style": ["consistent"], "vibe": ["australian_legend"] },
  "blurb": "Broc Parkes raced at the highest level of World Superbikes for years — quietly impressive and always in the mix. A proper Aussie road racer.",
  "active": true
}
```

### HeadlinesScreen

- **[P2 — resolved]** `avatarSource` is now typed as `ImageSourcePropType | null` at line 44. ✅ Confirmed.
- No new findings. Avatar composite, prefetch guard, tab navigation all intact.

### HeadlinesListScreen

- No new findings. AU/world/custom tabs, pull-to-refresh, empty/error states present.

### HeadlinesSettingsScreen

- **[P2 — carry]** Track arrival toggle still grouped under "Notifications" section — same UX concern as prior reviews. Not addressed; still low priority.

### CalendarScreen

- Calendar aggregation: 94 events, 70 AU full-detail. ✓
- AU calendar cache updated 2026-07-17 (4 days old) — within acceptable range; GitHub Action refreshes Mondays.
- No new findings.

### QAScreen *(major new features this cycle)*

- **[P1]** `QA_SEARCH_URL` exported from `app/constants/api.ts` (line 40) is no longer used — `QAScreen` calls `sendAskChat()` which uses `ROADRACE_ASK_URL` directly. The old `QA_SEARCH_URL` (`/qa/search`) export is an orphan. `ROADRACE_ASK_URL` is the correct endpoint. Recommend removing `QA_SEARCH_URL` from `api.ts` to avoid future confusion (someone may attempt to add a direct fetch to `QA_SEARCH_URL` thinking it's the Ask endpoint).
- **[P2]** The "Official rule check?" section lives **inside** the `ask` tab panel, below the general Ask section (not a separate top-level tab). This is a deliberate layout choice — but users may miss it on smaller screens since they must scroll past the Ask result to reach it. Consider promoting "Rules" to a third tab or adding a sticky section header. Low urgency — note only.
- **[P2]** `MomsOnlineMeta` is a new type surfaced from `askChat.ts`. The `momsOnline.fullPdfUrl!` non-null assertion at QAScreen line 567 is safe because it's inside a conditional `{rulesMomsOnline.fullPdfUrl ? ... : null}` guard — but the `!` is still technically unnecessary and could mislead. Minor.
- Ask tab uses `sendAskChat(q)` → `ROADRACE_ASK_URL` → `POST /roadrace-ai/ask` with `mode: 'ask'` (default). Correct.
- Rules tab uses `sendAskChat(q, { mode: 'rules' })` → same endpoint with `mode: 'rules'`. Correct.
- Trivia does NOT use GPT — uses `QA_TRIVIA_URL` (`/qa/trivia`). ✓ Brief matches.
- `key={i}` on trivia options (line 658) is a minor anti-pattern (stable only because options never reorder mid-question). Not blocking.

### RiderCoachScreen

- Coach / Bike Setup tabs both route to `CoachChatScreen` with mode param. ✓
- `navigation.setParams({ seedDraftMessage: undefined })` after consumption — confirmed still present. ✓
- No new findings.

### CoachChatScreen

- **[P2 — resolved]** Message list `key` now uses `m.id` (stable UUID from `createChatMessage`). ✓
- Attachments supported: photo from library, camera, data file. Permission denied paths present. ✓
- 90-second fetch timeout on `/roadrace-ai/chat`. ✓
- Loading indicator + error bubble when coach call fails. ✓

### BikeSetupBasicsScreen *(new since MOBILE_OPS)*

- Hotspot diagram (`suspension-bike.png`) with 44px hit targets. Clean layout.
- `BikeSetupHotspotSheet` opens a bottom sheet with contextual bike-setup tip. Navigation to `CoachChat` with `seedDraftMessage` from FAQ tap. ✓
- `HOTSPOT_HIT = 44` meets iOS HIG minimum tap target. ✓
- No issues found.

### BikeSetupSheetScreen *(new)*

Not explicitly audited this cycle — not in core screen inventory for this review. Flagged for next cycle.

### RoadRacerAiFaqsScreen *(new)*

- FAQ search: filters `coachFaqs` + `bikeFaqs` in real-time via `useMemo`. ✓
- Tap FAQ → `CoachChat` with pre-filled `seedDraftMessage`. ✓
- Navigation uses `RiderCoachStackParams` typed nav — clean.
- No issues found.

### TrackWalkScreen

- **[P2 — resolved]** Speech recognition is now a module-scope singleton (`getSpeechRecognition()`). Only one `require` called on first use — prior P2-3 resolved. ✓
- **[P2 — carry]** `recheckArrival()` still fires on every `useFocusEffect` (fast-path when disabled, but still an AsyncStorage read). Acceptable for PoC; not worse.
- **[P2 — carry]** No success toast on "Send to Coach." Still missing.

### ImportTrackNotesScreen

- **[P2 — resolved]** `mediaTypes: ['images']` confirmed at line 91. ✓ (was deprecated `MediaTypeOptions.Images` in prior review).
- No new findings.

---

## Cross-cutting

### Navigation

- `getParent()` still appears in `ImportTrackNotesScreen` and `TrackWalkScreen` for cross-tab navigation — works correctly because these are stack screens inside the Tab navigator. No regression.
- `RoadRacerAiFaqsScreen` and `BikeSetupBasicsScreen` both use typed `NativeStackNavigationProp` — clean.

### AsyncStorage / Storage

- New keys from this cycle: none confirmed (onboarding keys pre-existing; track walk keys stable).
- STORAGE_KEYS constants file still not created. Now ~8+ named keys across 5+ modules. Still a P2 refactor candidate.

### API

- Rate limit on `/roadrace-ai`: **10 req / 15 min** — tightened from 30 in prior cycle. ✓ P1 from RR_REVIEW_2026-07-14 resolved.
- Endpoints in `api.ts`: clean. `QA_SEARCH_URL` is the only unused export.
- `ROADRACE_ASK_URL` (`/roadrace-ai/ask`) — correctly used by `sendAskChat`.
- `ROADRACE_CHAT_URL` (`/roadrace-ai/chat`) — correctly used by `coachChat.ts`.
- No hardcoded production URLs outside `api.ts`.

### Analytics

- **`expo-firebase-analytics` removed** — `analytics.ts` is now a lightweight stub (`console.log` in `__DEV__`, no-op in prod). Prior P2-1 (MOBILE_OPS_2026-07-17) fully resolved. ✓
- Bundle no longer includes Firebase SDK.

### Security

- No secrets in source. OpenAI key on Render only.
- `ROADRACE_ASK_URL` posts user text to `/roadrace-ai/ask`. Server validates on its side; rate limited.
- No new outbound hosts observed.

### Docs

- `PROJECT_STATUS_AND_PRE_PRODUCTION.md` endpoint doc now lists `POST /roadrace-ai/ask` — matches actual implementation. Prior P1 (doc mismatch `/chat` vs `/ask`) confirmed resolved. ✓
- Rate limit listed as `10 req / 15 min per IP` in PROJECT_STATUS — matches `server.js`. ✓

---

## Coding improvements

| Priority | Area | Finding |
|----------|------|---------|
| [P1] | Dead export | `QA_SEARCH_URL` in `app/constants/api.ts` is unused — only `ROADRACE_ASK_URL` is called. Remove to prevent confusion. |
| [P2] | Onboarding catalog | Missing AU riders: **Remy Gardner** (highest-value gap; first AU MotoGP champion), Broc Parkes, Bryan Staring. Proposed JSON rows in OnboardingScreen section above. |
| [P2] | QAScreen layout | "Official rule check?" subsection below Ask result may be missed on small screens. Consider promoting Rules to a third tab or a persistent section divider. |
| [P2] | Analytics | `analytics.ts` is now a working stub — if any analytics provider is chosen, add a feature flag guard so that the dev `console.log` path and the prod path are clearly separated. Currently any `__DEV__` build logs analytics; a CI build that is not `__DEV__` silently no-ops. Acceptable for PoC, but document the intent. |
| [P2] | Storage | Still no `STORAGE_KEYS` constants file. ~8 keys spread across 5 storage modules. Risk of silent key collision on refactor. |
| [P2] | Performance | TrackWalkScreen `recheckArrival()` on every focus — minor IO even when disabled. Carry from prior reviews. |
| [P2] | UX | No success feedback ("Send to Coach" in TrackWalkScreen). Carry from prior reviews. |

---

## Suggested fix order (for Cursor)

1. **[P1]** Remove `QA_SEARCH_URL` export from `app/constants/api.ts` (one-line delete). Verify `npx tsc --noEmit` still passes.
2. **[P2]** Add Remy Gardner (and optionally Broc Parkes) to `app/src/data/onboardingRiders.json` using the proposed JSON rows above. Run `node scripts/test-onboarding-facts.mjs` after.
3. **[P2]** Add `STORAGE_KEYS` constants object to a new `app/src/constants/storageKeys.ts`; migrate the 8 key strings to it; update imports.
4. **[P2]** Add a toast or animated success text to TrackWalkScreen after "Send to Coach" navigates successfully.
5. **[P2]** Consider promoting "Official rule check?" to a third QAScreen tab (`ask | rules | trivia`) for discoverability.

---

## Manual verify on Expo Go

- [ ] Q&A → Ask tab → type "Who won the 1967 TT" → confirm web-search reply with sources.
- [ ] Q&A → Ask tab → scroll down to "Official rule check?" → type "Do I need a National licence for club road race?" → confirm structured MoMS citation response.
- [ ] Rules reply → confirm "MoMS on MA website →" link opens Motorcycling Australia page.
- [ ] Q&A → Trivia → play to Track Guru (8+ correct) → confirm GOAT animation fires.
- [ ] Onboarding → type "Remy Gardner" (after catalog addition) → confirm taste-bridge blurb appears.
- [ ] Onboarding → type a completely unknown rider → confirm friendly fallback message appears.
- [ ] Coach tab → Bike Setup → tap hotspot on diagram → confirm bottom sheet opens with tip.
- [ ] Coach FAQs screen → tap FAQ item → confirm CoachChat opens with pre-filled draft.
- [ ] Track Walk → voice transcription → confirm single permission prompt; transcript appears.
- [ ] Track Walk → "Send to Coach" → confirm Coach tab opens; note: no success toast yet (P2).

---

## Resolved since RR_REVIEW_2026-07-14 (rev 2)

| Item | Status |
|------|--------|
| [P1] Rate limit on `/roadrace-ai` too generous (30 req/15 min) | ✅ Resolved — tightened to **10 req/15 min** (`api/server.js:19`) |
| [P1] `PROJECT_STATUS_AND_PRE_PRODUCTION.md` endpoint doc mismatch (`/chat` vs `/ask`) | ✅ Resolved — doc now correctly lists `POST /roadrace-ai/ask` |
| [P2] `avatarSource` typed as `any` in `HeadlinesScreen.tsx:44` | ✅ Resolved — now `ImageSourcePropType | null` |
| [P2] `analytics.ts` swallows all errors silently; `expo-firebase-analytics` bundle bloat | ✅ Resolved — replaced with lightweight stub; Firebase dependency removed |
| [P2] `ImportTrackNotesScreen.tsx:92` deprecated `MediaTypeOptions.Images` | ✅ Resolved — uses `mediaTypes: ['images']` |
| [P2] `CoachChatScreen` `key={i}` on message list | ✅ Resolved — uses stable `m.id` |
| [P2] Speech recognition `require` called multiple times per session | ✅ Resolved — `getSpeechRecognition()` module-scope singleton |

---

## Out of scope

- Pixel/visual QA, Maestro/E2E, TestFlight/EAS distribution
- Vercel deploy status (not probed this run — Render cold-start affected timing)
- Firebase project decision (dependency removed; decision point moot)
- `BikeSetupSheetScreen` deep audit (new screen; flagged for next cycle)
- Remy Gardner / Broc Parkes catalog additions (proposed here, not implemented by Hermes)
