# Legacy GPT files (do not upload)

These were copied from the Downloads pack for history only.

| File | Issue |
|------|--------|
| `CORE DIAGNOSTIC JSON PACK v2.json` | Invalid JSON (markdown fences inside) — superseded by `../core-diagnostic-pack-v2.1.json` |
| `CoreDiagnosticPack_V1.json` | Same — content merged into v2.1 |
| `RIDER SKILL INTERACTION LAYER.json` | Same — use `../rider-skill-interaction-layer.json` |
| `SESSION LEARNING JSON (v1).json` | Same — use `../session-learning-v1.json` |
| `gpt-actions-fix.md` | Custom GPT Actions / Code Interpreter ops note — not coaching KB |

Do not re-upload these into ChatGPT Knowledge.
